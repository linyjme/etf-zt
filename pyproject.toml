[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "local-t-monitor"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
t-monitor = "etf_rotation.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
