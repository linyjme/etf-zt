from __future__ import annotations


SWING_PAGE = r"""<!doctype html>
<html lang="zh-CN">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>指数ETF波段监控</title>
<style>
:root{color-scheme:dark;--bg:#07111f;--panel:#101d30;--panel2:#0a1626;--line:#293d58;--text:#e9f0fa;--muted:#91a3bb;--blue:#65a8ff;--green:#39d69f;--yellow:#f2bd4e;--red:#ff6570;--purple:#b88cff}
*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at top,#152945,var(--bg) 46%);color:var(--text);font:14px/1.5 system-ui,"Microsoft YaHei",sans-serif}button,input,select{font:inherit}a{color:#acd1ff}.shell{max-width:1480px;margin:auto;padding:22px 18px 36px}nav[aria-label="监控模式"]{display:flex;gap:8px;margin-bottom:18px}nav[aria-label="监控模式"] a{border:1px solid var(--line);border-radius:999px;padding:6px 12px;text-decoration:none}nav[aria-label="监控模式"] a[aria-current=page]{border-color:var(--blue);background:#18345a;color:white}.topbar{display:flex;justify-content:space-between;align-items:flex-start;gap:16px;margin-bottom:14px}.topbar h1{font-size:27px;margin:0}.subtitle,.muted,.help{color:var(--muted)}.guard{display:flex;gap:10px;align-items:center;flex-wrap:wrap;justify-content:flex-end}.badge{border:1px solid #42658a;border-radius:99px;padding:5px 10px;color:#b9d9ff}.button{border:1px solid #3c6ca7;border-radius:8px;background:#2366ad;color:white;padding:8px 12px;cursor:pointer}.button.secondary{background:#132842}.button.danger{border-color:#8f4350;background:#542631}.button:focus-visible,a:focus-visible,input:focus-visible,select:focus-visible,[tabindex="0"]:focus-visible{outline:3px solid #91c7ff;outline-offset:2px}.button:disabled{opacity:.55;cursor:not-allowed}.risk-strip,.card,.sidebar{border:1px solid var(--line);border-radius:14px;background:linear-gradient(145deg,#12223a,#0c1728);box-shadow:0 12px 38px #0004}.risk-strip{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:1px;margin-bottom:16px;overflow:hidden}.risk-cell{background:#0b1728;padding:13px 15px;min-width:0}.risk-cell span{display:block;color:var(--muted);font-size:12px}.risk-cell strong{display:block;margin-top:3px;font-size:18px;overflow-wrap:anywhere}.layout{display:grid;grid-template-columns:285px minmax(0,1fr);gap:16px;align-items:start}.sidebar{position:sticky;top:12px;padding:14px}.sidebar h2,.card h2,.card h3{margin:0}.watch-help{margin:6px 0 12px;font-size:12px;color:var(--muted)}.watchlist{display:grid;gap:7px}.watch-row{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:8px;align-items:center;border:1px solid transparent;border-radius:9px;background:#081421;padding:8px}.watch-row.selected{border-color:var(--blue)}.watch-select{border:0;background:transparent;color:var(--text);padding:0;text-align:left;cursor:pointer;min-width:0}.watch-select strong,.watch-select span{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.watch-select span{color:var(--muted);font-size:11px}.state-dot{display:inline-block;width:8px;height:8px;border-radius:50%;margin-right:6px;background:var(--muted)}.state-dot.red{background:var(--red)}.state-dot.yellow{background:var(--yellow)}.state-dot.green{background:var(--green)}.toggle-label{display:flex;align-items:center;gap:5px;color:var(--muted);font-size:12px}.main-stack{display:grid;gap:14px}.card{padding:16px;min-width:0}.alert-banner{border-color:#91454e;background:#371c27}.alert-banner:empty{display:none}#swing-errors{color:#ff9ca4;margin:8px 0;white-space:pre-wrap}#swing-errors:empty{display:none}#swing-live-status{min-height:1.5em;color:var(--muted)}#swing-live-status.success{color:#7beac8}.detail-head{display:flex;justify-content:space-between;gap:14px;align-items:flex-start}.title-symbol{font-size:12px;color:var(--muted)}.status-pill{border:1px solid var(--line);border-radius:999px;padding:4px 9px}.status-pill.paused{border-color:var(--yellow);color:#ffd774}.status-pill.ready{border-color:var(--green);color:#77edc7}.status-pill.observe{border-color:var(--blue);color:#acd1ff}.chart-layout{display:grid;grid-template-columns:minmax(0,1.65fr) minmax(260px,.7fr);gap:14px;margin-top:14px}.chart-panel{position:relative;min-width:0}.chart-title{display:flex;justify-content:space-between;gap:10px;flex-wrap:wrap}.legend{display:flex;gap:12px;flex-wrap:wrap;color:var(--muted);font-size:12px}.legend i{display:inline-block;width:14px;border-top:2px solid;margin-right:4px;vertical-align:middle}.legend .close{border-color:white}.legend .ma20{border-color:var(--yellow)}.legend .ma60{border-color:var(--blue)}.legend .entry{border-color:var(--green)}.legend .stop{border-color:var(--red)}.chart-svg{display:block;width:100%;height:340px;margin-top:8px;touch-action:none}.axis{stroke:#50647f}.grid{stroke:#273a53;stroke-width:1}.axis-label{fill:#90a3bc;font-size:11px}.close-line,.ma20-line,.ma60-line{fill:none;vector-effect:non-scaling-stroke}.close-line{stroke:white;stroke-width:2}.ma20-line{stroke:var(--yellow);stroke-width:2}.ma60-line{stroke:var(--blue);stroke-width:2}.entry-zone{fill:#39d69f22;stroke:var(--green);stroke-dasharray:5 4}.hard-stop{stroke:var(--red);stroke-width:2;stroke-dasharray:4 4}.trailing-stop{stroke:var(--purple);stroke-width:2;stroke-dasharray:7 4}.chart-cursor{stroke:#9cafc7;stroke-dasharray:3 3;display:none}.chart-tooltip{position:absolute;display:none;pointer-events:none;z-index:4;max-width:260px;border:1px solid #4d6684;border-radius:8px;background:#06101deb;padding:8px 10px;white-space:pre-line;font:12px ui-monospace,monospace}.plan{background:#081421;border-radius:10px;padding:13px}.plan dl{display:grid;grid-template-columns:1fr auto;gap:7px 10px;margin:12px 0}.plan dt{color:var(--muted)}.plan dd{margin:0;text-align:right}.paused-note{color:#ffd774}.evidence{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;margin-top:14px}.evidence div{background:#081421;border-radius:8px;padding:9px;overflow-wrap:anywhere}.evidence span{display:block;color:var(--muted);font-size:11px}.tabs{display:grid;grid-template-columns:repeat(4,auto);justify-content:start;gap:6px;border-bottom:1px solid var(--line);margin-bottom:12px}.tab{border:0;border-bottom:2px solid transparent;background:transparent;color:var(--muted);padding:8px 11px;cursor:pointer}.tab.active{border-color:var(--blue);color:white}.panel[hidden]{display:none}.form-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px}.field{display:grid;gap:4px}.field span{color:var(--muted);font-size:12px}.field input,.field select{width:100%;min-width:0;border:1px solid var(--line);border-radius:7px;background:#071321;color:var(--text);padding:8px}.form-actions{display:flex;gap:8px;align-items:center;margin-top:11px}.form-result{min-height:1.5em}.form-result.error{color:#ff9ca4}.form-result.success{color:#7beac8}.alert-list,.ledger-list{display:grid;gap:8px}.alert-item,.ledger-item{border:1px solid var(--line);border-radius:9px;background:#081421;padding:10px}.alert-actions{display:flex;gap:6px;margin-top:8px}.empty{padding:32px 14px;text-align:center;color:var(--muted)}.health-grid{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:8px}.health-grid div{background:#081421;border-radius:8px;padding:8px}.health-grid span{display:block;color:var(--muted);font-size:11px}
@media(max-width:850px){.risk-strip{grid-template-columns:repeat(2,minmax(0,1fr))}.layout,.chart-layout{grid-template-columns:1fr}.sidebar{position:static}.watchlist{grid-template-columns:repeat(2,minmax(0,1fr))}.health-grid{grid-template-columns:repeat(3,minmax(0,1fr))}}
@media(max-width:600px){.shell{padding:14px 10px 28px}.topbar,.detail-head{flex-direction:column}.guard{justify-content:flex-start}.risk-strip,.watchlist,.evidence{grid-template-columns:repeat(2,minmax(0,1fr))}.health-grid,.form-grid{grid-template-columns:1fr 1fr}.tabs{grid-template-columns:repeat(2,1fr)}.button,.form-actions .button{width:100%}.form-actions{align-items:stretch;flex-direction:column}.chart-svg{height:285px}}
</style>
</head>
<body>
<main class="shell">
  <nav aria-label="监控模式"><a href="/">做T监控</a><a href="/swing" aria-current="page">波段监控</a></nav>
  <header class="topbar"><div><h1>指数ETF波段监控</h1><div class="subtitle">趋势过滤 · 回调计划 · 手工成交确认</div></div><div class="guard"><span class="badge">仅监控，不自动交易</span><button id="notification-permission" class="button secondary" type="button">通知需点击授权</button></div></header>
  <section id="portfolio-risk" class="risk-strip" aria-label="组合风险"><div class="risk-cell"><span>组合权益</span><strong>—</strong></div><div class="risk-cell"><span>可用现金</span><strong>—</strong></div><div class="risk-cell"><span>权益仓位</span><strong>—</strong></div><div class="risk-cell"><span>计划总风险</span><strong>—</strong></div><div class="risk-cell"><span>今日候选</span><strong>—</strong></div></section>
  <div id="swing-errors" role="alert"></div>
  <div id="swing-live-status" aria-live="polite">正在载入波段状态</div>
  <div class="layout">
    <aside class="sidebar"><h2>波段标的</h2><p class="watch-help">只可启停已核验的六只ETF，不支持自由输入未知代码。</p><nav id="swing-watchlist" class="watchlist" aria-label="波段标的"><div class="empty">正在载入</div></nav></aside>
    <div class="main-stack">
      <section id="intraday-warning" class="card alert-banner" role="alert"></section>
      <section id="swing-detail" class="card"><div class="empty">正在载入波段状态</div></section>
      <section class="card" aria-label="健康状态"><h2>数据与执行状态</h2><div id="health-grid" class="health-grid"><div><span>服务健康</span>—</div><div><span>日线数据</span>—</div><div><span>行情状态</span>—</div><div><span>策略计算</span>—</div><div><span>组合账本</span>—</div><div><span>浏览器通知</span>—</div></div></section>
      <section class="card"><div class="tabs" role="tablist" aria-label="波段信息"><button id="alerts-tab" class="tab active" data-panel="alerts-panel" role="tab" aria-controls="alerts-panel" aria-selected="true" tabindex="0">提醒记录</button><button id="ledger-tab" class="tab" data-panel="ledger-panel" role="tab" aria-controls="ledger-panel" aria-selected="false" tabindex="-1">成交账本</button><button id="backtest-tab" class="tab" data-panel="backtest-panel" role="tab" aria-controls="backtest-panel" aria-selected="false" tabindex="-1">波段回测</button><button id="params-tab" class="tab" data-panel="params-panel" role="tab" aria-controls="params-panel" aria-selected="false" tabindex="-1">参数说明</button></div>
        <div id="alerts-panel" class="panel" role="tabpanel" aria-labelledby="alerts-tab" tabindex="0"><div id="alert-list" class="alert-list"><div class="empty">暂无提醒</div></div></div>
        <div id="ledger-panel" class="panel" role="tabpanel" aria-labelledby="ledger-tab" tabindex="0" hidden><h3>成交账本</h3><div id="ledger-list" class="ledger-list"><div class="empty">账户尚未初始化</div></div>
          <h3>初始化波段账户</h3><form id="portfolio-initialize-form"><div class="form-grid"><label class="field"><span>账户名称</span><input name="name" required maxlength="40" autocomplete="off"></label><label class="field"><span>初始现金（元）</span><input name="cash" required type="number" min="0" step="0.01"></label><label class="field"><span>单笔风险比例</span><input name="default_risk_per_trade" type="number" min="0.0001" max="1" step="0.0001" placeholder="留空使用服务配置"></label></div><fieldset><legend>已有持仓（可选，按100份整手录入）</legend><div id="initial-position-rows" class="form-grid"></div></fieldset><div class="help">初始化后不可重复。失败后会保留账户和全部持仓输入。</div><div class="form-actions"><button class="button" type="submit">初始化账户</button><span class="form-result" aria-live="polite"></span></div></form>
          <h3>确认实际成交</h3><form id="trade-form"><div class="form-grid"><label class="field"><span>标的</span><select name="symbol" required></select></label><label class="field"><span>方向</span><select name="side"><option value="BUY">买入</option><option value="SELL">卖出</option></select></label><label class="field"><span>份额（整手约束由服务校验）</span><input name="shares" type="number" min="1" step="1" required></label><label class="field"><span>成交价格</span><input name="price" type="number" min="0.001" step="0.001" required></label><label class="field"><span>费用</span><input name="fee" type="number" min="0" step="0.01" required></label><label class="field"><span>成交时间（含本地时区）</span><input name="executed_at" type="datetime-local" required></label><label class="field"><span>每份计划风险</span><input name="planned_risk_per_share" type="number" min="0" step="0.001"></label><label class="field"><span>退出原因（卖出可选）</span><select name="exit_reason"><option value="">无</option><option value="STOP_EXIT">止损退出</option></select></label></div><div class="help">只记录你已经在外部完成的实际成交；失败后会保留已输入内容。</div><div class="form-actions"><button class="button" type="submit">保存成交</button><span class="form-result" aria-live="polite"></span></div></form>
          <h3>冲正成交</h3><form id="reverse-form"><div class="form-grid"><label class="field"><span>成交事件 UUID</span><input name="event_id" required autocomplete="off" pattern="[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}"></label></div><div class="help">仅用于纠正误录，失败后会保留已输入内容。</div><div class="form-actions"><button class="button danger" type="submit">冲正记录</button><span class="form-result" aria-live="polite"></span></div></form>
        </div>
        <div id="backtest-panel" class="panel" role="tabpanel" aria-labelledby="backtest-tab" tabindex="0" hidden><h3>波段回测</h3><div class="empty">回测结果将在独立回测模块完成后显示；这里不使用演示数字。</div></div>
        <div id="params-panel" class="panel" role="tabpanel" aria-labelledby="params-tab" tabindex="0" hidden><h3>参数说明</h3><p class="muted">指标使用完成日线。价格线显示原始价格；MA20、MA60 由复权收盘计算后按最新原始价/复权价比例映射。所有候选均需通过数据、日历和组合风险门槛。</p></div>
      </section>
    </div>
  </div>
</main>
<script>
/* SWING_PAGE_HELPERS_START */
function createPageState(){return {snapshot:null,snapshotRevision:-1,auxiliary:null,dailyBySymbol:new Map(),dailyRevisions:new Map(),selectedSymbol:null,safetyPaused:true,selectedExecutable:false,executionPaused:true,lastFormalPlan:null,lastFormalPlans:new Map(),failureSources:new Set(),failureMessages:new Map(),streamConnected:false}}
function cloneJson(value){return value==null?value:JSON.parse(JSON.stringify(value))}
function validSymbol(value){return typeof value==='string'&&/^[0-9]{6}$/.test(value)}
function validAlertId(value){return typeof value==='string'&&/^[0-9a-f]{24}$/.test(value)}
function validEventId(value){return typeof value==='string'&&/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/.test(value)}
function escapeHtml(value){return String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]))}
function finite(value){if(typeof value==='number')return Number.isFinite(value)?value:null;if(typeof value!=='string'||value===''||value.trim()!==value||!/^[+-]?(?:\d+\.?\d*|\.\d+)(?:[eE][+-]?\d+)?$/.test(value))return null;const number=Number(value);return Number.isFinite(number)?number:null}
function validDate(value){return typeof value==='string'&&/^\d{4}-\d{2}-\d{2}$/.test(value)&&!Number.isNaN(Date.parse(value+'T00:00:00Z'))}
function applyDailyPayload(state,payload){
  if(!payload||!validSymbol(payload.symbol))throw new Error('日线标的无效');
  const revision=Number(payload.revision);if(!Number.isInteger(revision)||revision<0)throw new Error('日线版本无效');
  if(!Array.isArray(payload.upserts))throw new Error('日线增量无效');
  const prior=Number(state.dailyRevisions.get(payload.symbol)??-1);
  if(!payload.reset&&revision<prior)return false;
  const bars=payload.reset?new Map():new Map(state.dailyBySymbol.get(payload.symbol)||[]);
  for(const raw of payload.upserts){if(!raw||!validDate(raw.trading_date)||raw.symbol&&raw.symbol!==payload.symbol)throw new Error('日线记录无效');bars.set(raw.trading_date,cloneJson(raw))}
  state.dailyBySymbol.set(payload.symbol,bars);state.dailyRevisions.set(payload.symbol,revision);return true;
}
function applySnapshotPayload(state,payload,authoritativeReset=false){
  if(!payload||!Array.isArray(payload.items))throw new Error('波段快照无效');
  const revision=Number(payload.revision);if(!Number.isInteger(revision)||revision<0)throw new Error('快照版本无效');
  if(!authoritativeReset&&revision<state.snapshotRevision)return false;
  for(const item of payload.items){if(!item||!validSymbol(item.symbol))throw new Error('快照标的无效')}
  const selected=state.selectedSymbol,incoming=cloneJson(payload);if(state.failureSources.size||!payloadSafetyReady(incoming))stripIntradaySnapshot(incoming);state.snapshot=incoming;state.snapshotRevision=revision;
  if(selected&&payload.items.some(item=>item.symbol===selected))state.selectedSymbol=selected;
  else if(!selected||!payload.items.some(item=>item.symbol===selected))state.selectedSymbol=payload.items[0]?.symbol||selected||null;
  for(const item of payload.items){if(item.formal_decision){const plan={symbol:item.symbol,as_of_trading_date:item.signal_data_date||payload.as_of_trading_date||null,formal_state:item.formal_state,formal_decision:cloneJson(item.formal_decision)};state.lastFormalPlans.set(item.symbol,plan);if(item.symbol===state.selectedSymbol)state.lastFormalPlan=plan}}
  refreshExecutionPaused(state);
  return true;
}
function stripIntradaySnapshot(snapshot){if(!snapshot)return snapshot;for(const item of snapshot.items||[])item.intraday_overlay=null;const overlayStates=new Set(['APPROACHING_ENTRY_ZONE','PREDEFINED_STOP_TOUCHED']);snapshot.active_alerts=(snapshot.active_alerts||[]).filter(alert=>alert?.scope!=='INTRADAY'&&!overlayStates.has(alert?.state));snapshot.alerts=(snapshot.alerts||[]).filter(alert=>alert?.scope!=='INTRADAY'&&!overlayStates.has(alert?.state));return snapshot}
function revokeIntraday(state,reason){
  state.safetyPaused=true;state.selectedExecutable=false;state.executionPaused=true;
  if(!state.snapshot)return;
  const selected=state.selectedSymbol||state.snapshot.items?.[0]?.symbol;
  const selectedItem=(state.snapshot.items||[]).find(item=>item.symbol===selected);
  if(selectedItem)state.lastFormalPlan={symbol:selectedItem.symbol,as_of_trading_date:selectedItem.signal_data_date||state.snapshot.as_of_trading_date||null,formal_state:selectedItem.formal_state,formal_decision:cloneJson(selectedItem.formal_decision||{}),paused_reason:reason};
  stripIntradaySnapshot(state.snapshot);
}
function shouldNotifyAlert(alert,userOptedIn,permission,seen,safetyPaused=false){return Boolean(!safetyPaused&&userOptedIn&&permission==='granted'&&alert&&alert.alert_id&&!seen.has(alert.alert_id)&&!alert.retracted&&(alert.currently_active===true||alert.active===true||alert.active_notification===true))}
function payloadSafetyReady(payload){const h=payload?.health||{},required={service:'OK',configuration:'OK',daily:'OK',strategy:'OK',portfolio:'OK',alerts:'OK',intraday:'REALTIME'};return Object.entries(required).every(([key,value])=>h[key]===value)&&Object.keys(payload?.errors||{}).length===0}
function snapshotExecutionReady(state){const item=(state.snapshot?.items||[]).find(value=>value.symbol===state.selectedSymbol);return Boolean(item&&item.execution_status==='READY_TO_EXECUTE')}
function refreshExecutionPaused(state){state.safetyPaused=state.failureSources.size>0||!payloadSafetyReady(state.snapshot);state.selectedExecutable=!state.safetyPaused&&snapshotExecutionReady(state);state.executionPaused=!state.selectedExecutable;if(state.safetyPaused&&state.snapshot)stripIntradaySnapshot(state.snapshot);return state.executionPaused}
function executionStatusCopy(state){return state.safetyPaused?'暂停执行：实时/数据/组合门槛未全部恢复':state.selectedExecutable?'执行候选已就绪，仍需手工确认':'数据正常，当前仅观察/无执行候选'}
function eventStreamOpened(state){state.streamConnected=true;return refreshExecutionPaused(state)}
function acceptSseSnapshot(state,payload,reset){const revision=Number(payload?.revision);if(!Number.isInteger(revision)||revision<0||!reset&&revision<state.snapshotRevision)return false;if(payloadSafetyReady(payload)){state.failureSources.delete('sse');state.failureSources.delete('snapshot');state.failureMessages.delete('sse');state.failureMessages.delete('snapshot')}if(reset){state.dailyBySymbol.clear();state.dailyRevisions.clear()}if(!applySnapshotPayload(state,payload,reset))return false;refreshExecutionPaused(state);return true}
function validateReadModelBundle(state,payload,bundle,authoritativeReset=false){if(!payload||!Array.isArray(payload.items))throw new Error('波段快照无效');const revision=Number(payload.revision);if(!Number.isInteger(revision)||revision<0)throw new Error('快照版本无效');if(!authoritativeReset&&revision<state.snapshotRevision)return false;for(const item of payload.items)if(!item||!validSymbol(item.symbol))throw new Error('快照标的无效');if(!bundle||!bundle.watch||!bundle.portfolio||!bundle.alerts)throw new Error('波段读模型不完整');for(const view of [bundle.watch,bundle.portfolio,bundle.alerts])if(Number(view.revision)!==revision)throw new Error('波段读模型与快照版本不一致');return true}
function applyReadModelBundle(state,payload,bundle,authoritativeReset=false){if(!validateReadModelBundle(state,payload,bundle,authoritativeReset))return false;const auxiliary=cloneJson(bundle);if(!applySnapshotPayload(state,payload,authoritativeReset))return false;state.auxiliary=auxiliary;return true}
function boundedAlerts(items,limit=80){const source=Array.isArray(items)?items.filter(Boolean):[],maximum=Number.isInteger(limit)&&limit>0?limit:80,active=source.filter(item=>item.currently_active===true||item.active===true||item.active_notification===true),activeIds=new Set(active.map(item=>item.alert_id)),history=source.filter(item=>!activeIds.has(item.alert_id));return active.slice(0,maximum).concat(history.slice(-Math.max(0,maximum-active.length))).slice(0,maximum)}
function isTemporaryIntradayAlert(scope,alertState){return scope==='INTRADAY'||alertState==='APPROACHING_ENTRY_ZONE'||alertState==='PREDEFINED_STOP_TOUCHED'}
function alertsForDisplay(items,state){const source=Array.isArray(items)?items.filter(Boolean):[];if(!state?.safetyPaused&&!state?.failureSources?.size)return source;return source.filter(alert=>!isTemporaryIntradayAlert(alert.scope,alert.state))}
function canActOnAlert(state,scope,alertState){return !((state?.safetyPaused||state?.failureSources?.size)&&isTemporaryIntradayAlert(scope,alertState))}
function pruneRememberedAlertIds(seen,activeIds,limit=256){const maximum=Number.isInteger(limit)&&limit>0?limit:256,keep=[],added=new Set();for(const id of activeIds||[]){if(seen.has(id)&&!added.has(id)&&keep.length<maximum){keep.push(id);added.add(id)}}for(const id of [...seen].reverse()){if(!added.has(id)&&keep.length<maximum){keep.push(id);added.add(id)}}seen.clear();for(const id of keep.reverse())seen.add(id);return seen}
function rollingAdjusted(bars,windowSize,index){if(index+1<windowSize)return null;let sum=0;for(let i=index-windowSize+1;i<=index;i++){const value=finite(bars[i].adjusted_close);if(value==null)return null;sum+=value}return sum/windowSize}
function chartSeries(source){const bars=[...source].filter(bar=>validDate(bar.trading_date)&&finite(bar.close)!=null&&finite(bar.adjusted_close)!=null).sort((a,b)=>a.trading_date.localeCompare(b.trading_date));if(!bars.length)return {points:[],scale:null};const latest=bars[bars.length-1],scale=latest.close/latest.adjusted_close;if(!Number.isFinite(scale)||scale<=0)return {points:[],scale:null};const all=bars.map((bar,index)=>({date:bar.trading_date,close:finite(bar.close),ma20:(rollingAdjusted(bars,20,index)??NaN)*scale,ma60:(rollingAdjusted(bars,60,index)??NaN)*scale}));return {points:all.slice(-120),scale}}
function stableJson(value){if(Array.isArray(value))return '['+value.map(stableJson).join(',')+']';if(value&&typeof value==='object')return '{'+Object.keys(value).sort().map(key=>JSON.stringify(key)+':'+stableJson(value[key])).join(',')+'}';return JSON.stringify(value)}
function prepareIntent(registry,owner,path,payload,keyFactory=()=>crypto.randomUUID()){const fingerprint=path+'\n'+stableJson(payload),existing=registry.get(owner);if(existing&&existing.fingerprint===fingerprint)return existing;const record={fingerprint,key:keyFactory(),promise:null};registry.set(owner,record);return record}
function settleIntentSuccess(registry,owner,key){if(registry.get(owner)?.key===key)registry.delete(owner)}
function buildInitialPositions(rows){const result={};for(const row of rows){if(!row.enabled)continue;if(!validSymbol(row.symbol))throw new Error('已有持仓标的无效');const shares=finite(row.shares),cost=finite(row.average_cost),risk=row.planned_risk_per_share===''?0:finite(row.planned_risk_per_share);if(!Number.isInteger(shares)||shares<=0||shares%100!==0)throw new Error(`${row.symbol}份额必须是100的正整数倍`);if(cost==null||cost<=0)throw new Error(`${row.symbol}平均成本必须大于0`);if(risk==null||risk<0)throw new Error(`${row.symbol}每份计划风险不得为负`);result[row.symbol]={shares,average_cost:cost,planned_risk_per_share:risk}}return result}
function pointerIndex(clientX,plotLeft,plotWidth,count){if(!Number.isFinite(clientX)||!Number.isFinite(plotLeft)||!Number.isFinite(plotWidth)||plotWidth<=0||!Number.isInteger(count)||count<=0)return 0;const ratio=Math.max(0,Math.min(1,(clientX-plotLeft)/plotWidth));return Math.round(ratio*Math.max(0,count-1))}
/* SWING_PAGE_HELPERS_END */

const VERIFIED_SYMBOLS=[['510300','沪深300ETF'],['510500','中证500ETF'],['563360','中证A500ETF'],['512100','中证1000ETF'],['159915','创业板ETF'],['588000','科创50ETF']];
const STATE_LABELS={EXIT_CANDIDATE:'退出候选',REDUCE_CANDIDATE:'减仓候选',ADD_CANDIDATE:'加仓候选',TRIAL_ENTRY_CANDIDATE:'试仓候选',PULLBACK_WATCH:'回调观察',UPTREND_WATCH:'趋势观察',NOT_ELIGIBLE:'暂不符合',BLOCKED:'已阻断',INSUFFICIENT_HISTORY:'数据不足'};
const OVERLAY_LABELS={APPROACHING_ENTRY_ZONE:'接近计划买入区',PREDEFINED_STOP_TOUCHED:'预先确定止损位已触及'};
const GET_TIMEOUT_MS=8000,POST_TIMEOUT_MS=15000,AUXILIARY_ATTEMPTS=3,ALERT_DOM_LIMIT=80,NOTIFIED_ID_LIMIT=256;
const state=createPageState(),notifiedAlertIds=new Set(),knownActiveAlertIds=new Set(),intentRegistry=new Map();
let notificationOptIn=false,snapshotRequest=0,dailyRequest=0,watchlistPayload={items:[]},portfolioPayload=null,alertsPayload={items:[]},eventSource=null,actionStatusTimer=null;
const riskNode=document.querySelector('#portfolio-risk'),watchNode=document.querySelector('#swing-watchlist'),detailNode=document.querySelector('#swing-detail'),errorsNode=document.querySelector('#swing-errors'),liveNode=document.querySelector('#swing-live-status'),warningNode=document.querySelector('#intraday-warning'),healthNode=document.querySelector('#health-grid'),alertNode=document.querySelector('#alert-list'),ledgerNode=document.querySelector('#ledger-list'),permissionButton=document.querySelector('#notification-permission');
const fmt=(value,digits=2)=>finite(value)==null?'—':Number(value).toLocaleString('zh-CN',{minimumFractionDigits:digits,maximumFractionDigits:digits});
const pct=value=>finite(value)==null?'—':(Number(value)*100).toFixed(2)+'%';
const text=value=>value==null||value===''?'—':String(value);
function updateErrors(){errorsNode.textContent=[...state.failureMessages.values()].join('\n');if(errorsNode.textContent){liveNode.className='';liveNode.textContent=errorsNode.textContent}else if(!liveNode.classList.contains('success'))liveNode.textContent='数据已同步'}
function setError(message=''){if(message)state.failureMessages.set('action',message);else state.failureMessages.delete('action');updateErrors()}
function setActionStatus(message,ok){if(actionStatusTimer){clearTimeout(actionStatusTimer);actionStatusTimer=null}if(!ok){setError(message);return}state.failureMessages.delete('action');updateErrors();liveNode.textContent=message;liveNode.className='form-result success';actionStatusTimer=setTimeout(()=>{liveNode.className='';liveNode.textContent=state.failureMessages.size?[...state.failureMessages.values()].join('\n'):'数据已同步';actionStatusTimer=null},3000)}
function markFailure(source,message){state.failureSources.add(source);state.failureMessages.set(source,message);revokeIntraday(state,message);refreshExecutionPaused(state);updateErrors();renderAll()}
function markRecovered(source){state.failureSources.delete(source);state.failureMessages.delete(source);refreshExecutionPaused(state);updateErrors()}
function candidatePriority(item){const stateName=item.formal_state;return stateName==='EXIT_CANDIDATE'?0:item.intraday_overlay==='PREDEFINED_STOP_TOUCHED'?0:['REDUCE_CANDIDATE','ADD_CANDIDATE','TRIAL_ENTRY_CANDIDATE'].includes(stateName)?1:stateName==='PULLBACK_WATCH'?2:stateName==='UPTREND_WATCH'?3:4}
function renderRisk(){const p=state.snapshot?.portfolio;const equity=finite(p?.equity),market=finite(p?.etf_market_value),exposure=equity&&market!=null?market/equity:null;const candidates=(state.snapshot?.items||[]).filter(item=>String(item.formal_state).endsWith('_CANDIDATE')).length;riskNode.innerHTML=`<div class="risk-cell"><span>组合权益</span><strong>${fmt(equity)}</strong></div><div class="risk-cell"><span>可用现金</span><strong>${fmt(p?.cash)}</strong></div><div class="risk-cell"><span>权益仓位</span><strong>${pct(exposure)}</strong></div><div class="risk-cell"><span>计划总风险</span><strong>${fmt(p?.planned_risk)} / ${equity==null?'—':fmt(equity*.02)}</strong></div><div class="risk-cell"><span>今日候选</span><strong>${candidates}</strong></div>`}
function itemFor(symbol){return (state.snapshot?.items||[]).find(item=>item.symbol===symbol)||null}
function renderWatchlist(){
  watchNode.replaceChildren();const enabled=new Map((watchlistPayload.items||[]).filter(item=>validSymbol(item.symbol)).map(item=>[item.symbol,item.enabled===true]));
  const ordered=[...VERIFIED_SYMBOLS].sort((a,b)=>candidatePriority(itemFor(a[0])||{})-candidatePriority(itemFor(b[0])||{}));
  for(const [symbol,name] of ordered){const row=document.createElement('div');row.className='watch-row'+(symbol===state.selectedSymbol?' selected':'');const select=document.createElement('button');select.type='button';select.className='watch-select';select.dataset.symbol=symbol;const info=itemFor(symbol),stateName=info?.formal_state||'NOT_ELIGIBLE',overlay=info?.intraday_overlay?.overlay||info?.intraday_overlay;const dot=document.createElement('i');dot.className='state-dot '+(overlay==='PREDEFINED_STOP_TOUCHED'||stateName==='EXIT_CANDIDATE'?'red':['REDUCE_CANDIDATE','ADD_CANDIDATE','TRIAL_ENTRY_CANDIDATE','PULLBACK_WATCH'].includes(stateName)?'yellow':stateName==='UPTREND_WATCH'?'green':'');dot.setAttribute('aria-hidden','true');const strong=document.createElement('strong');strong.textContent=name;const small=document.createElement('span');small.textContent=`${symbol} · ${STATE_LABELS[stateName]||stateName}`;select.append(dot,strong,small);select.addEventListener('click',()=>selectSymbol(symbol));const label=document.createElement('label');label.className='toggle-label';const toggle=document.createElement('input');toggle.type='checkbox';toggle.checked=enabled.get(symbol)===true;toggle.setAttribute('aria-label',`${name}监控开关`);toggle.addEventListener('change',()=>toggleWatch(symbol,toggle.checked,toggle));label.append(toggle,document.createTextNode('监控'));row.append(select,label);watchNode.append(row)}
}
function chartHtml(symbol){
  const bars=[...(state.dailyBySymbol.get(symbol)?.values()||[])];
  if(!bars.length)return '<div class="empty">无日线数据：等待已完成交易日行情</div>';
  if(bars.length<60)return `<div class="empty">日线数据不足：当前 ${bars.length} 根，至少需要 60 根计算 MA60</div>`;
  const series=chartSeries(bars),points=series.points,latest=bars.sort((a,b)=>a.trading_date.localeCompare(b.trading_date))[bars.length-1],adjustment_scale=latest.close/latest.adjusted_close;
  if(!Number.isFinite(adjustment_scale)||adjustment_scale<=0)return '<div class="empty">日线复权比例无效，图表暂停</div>';
  const decision=itemFor(symbol)?.formal_decision||state.lastFormalPlans.get(symbol)?.formal_decision||{},evidence=decision.evidence||{};
  const entryLow=finite(decision.planned_entry_low??evidence.entry_zone_low_raw),entryHigh=finite(decision.planned_entry_high??evidence.entry_zone_high_raw),hardStop=finite(evidence.hard_stop_raw_mapped??evidence.initial_stop_raw),trailingStop=finite(evidence.trailing_stop_raw),w=860,h=340,left=62,right=18,top=18,bottom=38,pw=w-left-right,ph=h-top-bottom;
  const values=points.flatMap(point=>[point.close,point.ma20,point.ma60]).concat([entryLow,entryHigh,hardStop,trailingStop]).filter(Number.isFinite),lo=Math.min(...values),hi=Math.max(...values),span=hi-lo||1,x=index=>left+index*pw/Math.max(1,points.length-1),y=value=>top+(hi-value)*ph/span,path=key=>points.map((point,index)=>Number.isFinite(point[key])?`${index&&Number.isFinite(points[index-1][key])?'L':'M'}${x(index).toFixed(1)} ${y(point[key]).toFixed(1)}`:'').join(' ');
  const yTicks=Array.from({length:5},(_,index)=>{const value=hi-span*index/4,py=y(value);return `<g><line class="grid" x1="${left}" y1="${py}" x2="${w-right}" y2="${py}"/><text class="axis-label" x="${left-7}" y="${py+4}" text-anchor="end">${escapeHtml(fmt(value,3))}</text></g>`}).join('');
  const tickIndexes=[0,Math.floor((points.length-1)/2),points.length-1],xTicks=tickIndexes.map(index=>`<g><line class="axis" x1="${x(index)}" y1="${h-bottom}" x2="${x(index)}" y2="${h-bottom+5}"/><text class="axis-label" x="${x(index)}" y="${h-8}" text-anchor="middle">${escapeHtml(points[index].date.slice(5))}</text></g>`).join('');
  const horizontal=(value,cls,label)=>value==null?'':`<g><line class="${cls}" x1="${left}" y1="${y(value)}" x2="${w-right}" y2="${y(value)}"/><text class="axis-label" x="${w-right-3}" y="${y(value)-4}" text-anchor="end">${escapeHtml(label)}</text></g>`;
  const entry=entryLow==null||entryHigh==null?'':`<rect class="entry-zone" x="${left}" y="${y(entryHigh)}" width="${pw}" height="${Math.max(1,y(entryLow)-y(entryHigh))}"/>`;
  window.swingChartPoints=points;
  return `<div class="chart-title"><strong>最近120个完成交易日</strong><span class="muted">原始价格轴</span></div><div class="legend"><span><i class="close"></i>原始收盘</span><span><i class="ma20"></i>MA20</span><span><i class="ma60"></i>MA60</span><span><i class="entry"></i>计划买入区</span><span>硬止损 / 移动止损</span></div><svg id="swing-chart" class="chart-svg" role="img" aria-label="${escapeHtml(symbol)}最近完成日线、均线、计划买入区与止损线" tabindex="0" viewBox="0 0 ${w} ${h}">${yTicks}${entry}${horizontal(hardStop,'hard-stop','硬止损')}${horizontal(trailingStop,'trailing-stop','移动止损')}<path class="close-line" d="${path('close')}"/><path class="ma20-line" d="${path('ma20')}"/><path class="ma60-line" d="${path('ma60')}"/><line id="chart-cursor" class="chart-cursor" y1="${top}" y2="${h-bottom}"/><g>${xTicks}</g><rect id="chart-hit" x="${left}" y="${top}" width="${pw}" height="${ph}" fill="transparent" data-left="${left}" data-width="${pw}"/></svg><div id="chart-tooltip" class="chart-tooltip" role="status"></div>`;
}
function planHtml(item){const decision=item?.formal_decision||state.lastFormalPlans.get(state.selectedSymbol)?.formal_decision||{},evidence=decision.evidence||{},date=item?.signal_data_date||state.lastFormalPlans.get(state.selectedSymbol)?.as_of_trading_date,copy=executionStatusCopy(state);return `<div class="plan"><h3>下一交易日计划</h3><p class="${state.safetyPaused?'paused-note':'muted'}">${escapeHtml(copy)}</p><dl><dt>信号数据日期</dt><dd>${escapeHtml(text(date))}</dd><dt>买入区</dt><dd>${fmt(decision.planned_entry_low,3)} – ${fmt(decision.planned_entry_high,3)}</dd><dt>建议份额</dt><dd>${fmt(decision.planned_shares,0)}</dd><dt>预计资金</dt><dd>${finite(decision.planned_shares)!=null&&finite(decision.planned_entry_high)!=null?fmt(decision.planned_shares*decision.planned_entry_high):'—'}</dd><dt>初始/保护止损</dt><dd>${fmt(decision.planned_stop??evidence.protective_stop_raw,3)}</dd><dt>本次风险</dt><dd>${pct(decision.planned_risk_rate)}</dd><dt>2R 减仓位</dt><dd>${fmt(decision.first_reduce_price,3)}</dd></dl></div>`}
function renderDetail(){
  const item=itemFor(state.selectedSymbol);if(!item){detailNode.innerHTML='<div class="empty">所选标的尚无正式状态；可在左侧启用并等待日线生产者发布。</div>';return}
  const decision=item.formal_decision||{},evidence=decision.evidence||{},blocked=(item.blocked_reasons||decision.blocked_reasons||[]),overlay=item.intraday_overlay?.overlay||item.intraday_overlay;
  const statusClass=state.safetyPaused?'paused':state.selectedExecutable?'ready':'observe';
  detailNode.innerHTML=`<div class="detail-head"><div><div class="title-symbol">${escapeHtml(item.symbol)}</div><h2>${escapeHtml(item.name||item.symbol)}</h2><div class="muted">信号数据日期 ${escapeHtml(text(item.signal_data_date))} · 盘中价格时间 ${escapeHtml(text(item.current_price_time))}</div></div><span class="status-pill ${statusClass}">${escapeHtml(executionStatusCopy(state))}</span></div>${overlay?`<p class="alert-banner">${escapeHtml(OVERLAY_LABELS[overlay]||overlay)}</p>`:''}<div class="chart-layout"><div class="chart-panel">${chartHtml(item.symbol)}</div>${planHtml(item)}</div><div class="evidence"><div><span>正式状态</span>${escapeHtml(STATE_LABELS[item.formal_state]||item.formal_state)}</div><div><span>MA20</span>${fmt(evidence.ma20_raw,3)}</div><div><span>MA60</span>${fmt(evidence.ma60_raw,3)}</div><div><span>ATR14</span>${fmt(evidence.atr14_raw,3)}</div><div><span>距 MA20</span>${fmt(evidence.close_ma20_distance_raw,3)}</div><div><span>风险预算</span>${pct(decision.planned_risk_rate)}</div><div><span>盘中行情状态</span>${escapeHtml(text(item.intraday_health_status))}</div><div><span>执行状态</span>${escapeHtml(text(item.execution_status))}</div><div><span>阻断原因</span>${blocked.length?blocked.map(escapeHtml).join('；'):'—'}</div></div>`;
  bindChartTooltip();
}
function bindChartTooltip(){const svg=document.querySelector('#swing-chart'),hit=document.querySelector('#chart-hit'),tooltip=document.querySelector('#chart-tooltip'),cursor=document.querySelector('#chart-cursor'),points=window.swingChartPoints||[];if(!svg||!hit||!tooltip||!cursor||!points.length)return;let index=points.length-1;const show=(next,clientX,clientY)=>{index=Math.max(0,Math.min(points.length-1,next));const point=points[index],left=Number(hit.dataset.left),width=Number(hit.dataset.width),x=left+index*width/Math.max(1,points.length-1);cursor.setAttribute('x1',x);cursor.setAttribute('x2',x);cursor.style.display='block';tooltip.textContent=`${point.date}\n收盘 ${fmt(point.close,3)}\nMA20 ${fmt(point.ma20,3)}\nMA60 ${fmt(point.ma60,3)}`;tooltip.style.display='block';const rect=svg.getBoundingClientRect();tooltip.style.left=`${Math.max(4,(clientX??rect.left+x*rect.width/860)-rect.left+8)}px`;tooltip.style.top=`${Math.max(4,(clientY??rect.top+24)-rect.top+8)}px`};const locate=clientX=>{const plot=hit.getBoundingClientRect();if(plot.width>0)return pointerIndex(clientX,plot.left,plot.width,points.length);const rect=svg.getBoundingClientRect(),viewWidth=svg.viewBox?.baseVal?.width||860,left=rect.left+Number(hit.dataset.left)*rect.width/viewWidth,width=Number(hit.dataset.width)*rect.width/viewWidth;return pointerIndex(clientX,left,width,points.length)};svg.addEventListener('pointermove',event=>show(locate(event.clientX),event.clientX,event.clientY));svg.addEventListener('pointerleave',()=>{cursor.style.display='none';tooltip.style.display='none'});svg.addEventListener('touchmove',event=>{const touch=event.touches[0];if(touch)show(locate(touch.clientX),touch.clientX,touch.clientY)},{passive:true});svg.addEventListener('focus',()=>show(index));svg.addEventListener('keydown',event=>{if(event.key==='ArrowLeft'||event.key==='ArrowRight'){event.preventDefault();show(index+(event.key==='ArrowRight'?1:-1))}})}
function renderHealth(){const h=state.snapshot?.health||{};const permission=('Notification'in window)?Notification.permission:'不支持';healthNode.innerHTML=`<div><span>服务健康</span>${escapeHtml(text(h.service))}</div><div><span>日线数据</span>${escapeHtml(text(h.daily))} · ${escapeHtml(text(state.snapshot?.as_of_trading_date))}</div><div><span>行情状态</span>${escapeHtml(text(h.intraday))}</div><div><span>策略计算</span>${escapeHtml(text(h.strategy))}</div><div><span>组合账本</span>${escapeHtml(text(h.portfolio))}</div><div><span>浏览器通知</span>${escapeHtml(permission)}</div>`}
function renderAlerts(){const source=alertsPayload?.items||state.snapshot?.alerts||[],items=boundedAlerts(alertsForDisplay(source,state).filter(alert=>!alert.retracted),ALERT_DOM_LIMIT);if(!items.length){alertNode.innerHTML='<div class="empty">暂无提醒</div>';return}alertNode.innerHTML=items.map(alert=>`<article class="alert-item"><strong>${escapeHtml(alert.label||alert.state)}</strong><div class="muted">${escapeHtml(alert.symbol)} · ${escapeHtml(alert.trading_date)} · ${escapeHtml(alert.scope)}</div><details><summary>查看判断依据</summary><pre>${escapeHtml(JSON.stringify(alert.evidence||{},null,2))}</pre></details><div class="alert-actions"><button class="button secondary" data-alert-action="acknowledge" data-alert-id="${escapeHtml(alert.alert_id)}" data-alert-scope="${escapeHtml(alert.scope)}" data-alert-state="${escapeHtml(alert.state)}" type="button">标记已读</button><button class="button secondary" data-alert-action="ignore" data-alert-id="${escapeHtml(alert.alert_id)}" data-alert-scope="${escapeHtml(alert.scope)}" data-alert-state="${escapeHtml(alert.state)}" type="button">忽略本次</button><button class="button" data-alert-trade="${escapeHtml(alert.symbol)}" data-alert-scope="${escapeHtml(alert.scope)}" data-alert-state="${escapeHtml(alert.state)}" type="button">记录成交</button></div><div class="form-result" aria-live="polite"></div></article>`).join('');for(const button of alertNode.querySelectorAll('[data-alert-action]'))button.addEventListener('click',()=>transitionAlert(button));for(const button of alertNode.querySelectorAll('[data-alert-trade]'))button.addEventListener('click',()=>openTradeForAlert(button.dataset.alertTrade,button.dataset.alertScope,button.dataset.alertState))}
function renderLedger(){const projection=portfolioPayload?.projection||state.snapshot?.portfolio;if(!projection){ledgerNode.innerHTML='<div class="empty">账户尚未初始化；请先完成初始化波段账户。</div>';return}const positions=Object.values(projection.positions||{});ledgerNode.innerHTML=`<div class="ledger-item"><strong>${escapeHtml(projection.name)}</strong><div class="muted">账本日期 ${escapeHtml(text(projection.as_of_trading_date))} · 最近事件 ${escapeHtml(text(projection.last_event_id))}</div><div>现金 ${fmt(projection.cash)} · 已实现损益 ${fmt(projection.realized_pnl)}</div></div>`+(positions.length?positions.map(position=>`<div class="ledger-item"><strong>${escapeHtml(position.symbol)}</strong><div>持仓 ${fmt(position.shares,0)} · 可卖 ${fmt(position.sellable_shares,0)} · 成本 ${fmt(position.average_cost,3)} · 计划风险 ${fmt(position.planned_risk)}</div></div>`).join(''):'<div class="empty">当前没有持仓记录</div>')}
function renderWarning(){warningNode.classList.toggle('alert-banner',state.safetyPaused);warningNode.classList.toggle('muted',!state.safetyPaused);if(state.safetyPaused){const date=state.lastFormalPlan?.as_of_trading_date||state.snapshot?.as_of_trading_date;warningNode.textContent=`执行已暂停。已撤销盘中临时提醒；保留 ${text(date)} 的最后正式计划供查看。`}else if(!state.selectedExecutable)warningNode.textContent='数据正常，当前仅观察/无执行候选。';else warningNode.textContent=''}
function renderAll(){renderRisk();renderWatchlist();renderDetail();renderHealth();renderAlerts();renderLedger();renderWarning()}
function selectSymbol(symbol){if(!validSymbol(symbol))return;state.selectedSymbol=symbol;refreshExecutionPaused(state);const tradeSelect=document.querySelector('#trade-form [name="symbol"]');if(tradeSelect)tradeSelect.value=symbol;renderAll();loadDaily(symbol).catch(()=>{})}
async function jsonResponse(response){let value;try{value=await response.json()}catch(_error){const error=new Error('服务返回了无法解析的数据');error.status=response.status;throw error}if(!response.ok){const error=new Error(value.message||value.error||'请求失败');error.status=response.status;throw error}return value}
async function fetchWithTimeout(path,options={},timeoutMs=GET_TIMEOUT_MS){const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),timeoutMs);try{return await fetch(path,{...options,signal:controller.signal})}catch(error){if(error?.name==='AbortError')throw new Error('请求超时，请重试');throw error}finally{clearTimeout(timer)}}
async function postLocalRecord(path,payload,{idempotencyKey=null}={}){const headers=idempotencyKey?{'Content-Type':'application/json','Idempotency-Key':idempotencyKey}:{'Content-Type':'application/json'};return jsonResponse(await fetchWithTimeout(path,{method:'POST',headers,body:JSON.stringify(payload)},POST_TIMEOUT_MS))}
async function submitIntent(owner,path,payload){const record=prepareIntent(intentRegistry,owner,path,payload);if(record.promise)return record.promise;record.promise=postLocalRecord(path,payload,{idempotencyKey:record.key});try{const result=await record.promise;settleIntentSuccess(intentRegistry,owner,record.key);return result}catch(error){record.promise=null;throw error}}
async function toggleWatch(symbol,enabled,control){if(!validSymbol(symbol)){control.checked=!enabled;return}control.disabled=true;try{await postLocalRecord('/api/swing/watchlist',{symbol,enabled});setActionStatus('监控列表已保存',true);await loadSnapshot(false)}catch(error){control.checked=!enabled;setActionStatus(`监控列表保存失败：${error.message}`,false)}finally{control.disabled=false}}
async function loadDaily(symbol){if(!validSymbol(symbol))throw new Error('日线标的无效');const token=++dailyRequest,since=state.dailyRevisions.get(symbol)??0;try{const payload=await jsonResponse(await fetchWithTimeout(`/api/swing/daily-quotes?symbol=${encodeURIComponent(symbol)}&since=${encodeURIComponent(String(since))}&limit=179`,{cache:'no-store'},GET_TIMEOUT_MS));if(token!==dailyRequest||symbol!==state.selectedSymbol)return;applyDailyPayload(state,payload);markRecovered('daily');renderAll()}catch(error){if(token===dailyRequest)markFailure('daily',`日线数据读取失败：${error.message}`);throw error}}
function syncActiveAlerts(payload){const current=(payload.active_alerts||[]).filter(alert=>alert&&alert.currently_active!==false&&!alert.retracted);for(const alert of current){const isNew=!knownActiveAlertIds.has(alert.alert_id);knownActiveAlertIds.add(alert.alert_id);if(isNew&&shouldNotifyAlert(alert,notificationOptIn,('Notification'in window)?Notification.permission:'denied',notifiedAlertIds,state.safetyPaused)){notifiedAlertIds.add(alert.alert_id);new Notification(`波段提醒 · ${alert.symbol}`,{body:alert.label||alert.state})}}const activeIds=new Set(current.map(alert=>alert.alert_id));for(const id of [...knownActiveAlertIds])if(!activeIds.has(id))knownActiveAlertIds.delete(id);pruneRememberedAlertIds(notifiedAlertIds,activeIds,NOTIFIED_ID_LIMIT);pruneRememberedAlertIds(knownActiveAlertIds,activeIds,NOTIFIED_ID_LIMIT)}
async function fetchAuxiliaryOnce(){const [watch,portfolio,alerts]=await Promise.all(['/api/swing/watchlist','/api/swing/portfolio','/api/swing/alerts'].map(path=>fetchWithTimeout(path,{cache:'no-store'},GET_TIMEOUT_MS).then(jsonResponse)));return {watch,portfolio,alerts}}
async function loadAuxiliary(expectedRevision){let lastError=null;for(let attempt=0;attempt<AUXILIARY_ATTEMPTS;attempt++){try{const bundle=await fetchAuxiliaryOnce();for(const view of [bundle.watch,bundle.portfolio,bundle.alerts])if(Number(view.revision)!==Number(expectedRevision))throw new Error(`读模型版本 ${view.revision} 尚未追上快照 ${expectedRevision}`);return bundle}catch(error){lastError=error;if(attempt+1<AUXILIARY_ATTEMPTS)await new Promise(resolve=>setTimeout(resolve,80*(2**attempt)))}}throw lastError||new Error('波段读模型同步失败')}
function syncAuxiliaryGlobals(){if(!state.auxiliary)return;watchlistPayload=state.auxiliary.watch;portfolioPayload=state.auxiliary.portfolio;alertsPayload=state.auxiliary.alerts}
function clearRecoveredSources(sources){for(const source of sources){state.failureSources.delete(source);state.failureMessages.delete(source)}}
function installReadModel(payload,bundle,reset,recoveredSources){if(!validateReadModelBundle(state,payload,bundle,reset))return false;clearRecoveredSources(recoveredSources);if(reset){state.dailyBySymbol.clear();state.dailyRevisions.clear()}if(!applyReadModelBundle(state,payload,bundle,reset))return false;syncAuxiliaryGlobals();refreshExecutionPaused(state);updateErrors();syncActiveAlerts(state.snapshot);renderAll();return true}
async function loadSnapshot(authoritative=true){const token=++snapshotRequest;try{const payload=await jsonResponse(await fetchWithTimeout('/api/swing/snapshot',{cache:'no-store'},GET_TIMEOUT_MS)),bundle=await loadAuxiliary(payload.revision);if(token!==snapshotRequest)return false;if(!installReadModel(payload,bundle,authoritative,['snapshot']))return false;if(state.selectedSymbol)await loadDaily(state.selectedSymbol).catch(()=>{});return true}catch(error){if(token===snapshotRequest)markFailure('snapshot',`波段快照读取失败：${error.message}`);return false}}
async function applySsePayload(payload,reset){const token=++snapshotRequest;try{const bundle=await loadAuxiliary(payload?.revision);if(token!==snapshotRequest)return false;if(!installReadModel(payload,bundle,reset,['sse','snapshot']))return false;if(state.selectedSymbol)loadDaily(state.selectedSymbol).catch(()=>{});return true}catch(error){if(token===snapshotRequest)markFailure('sse',`事件读模型同步失败：${error.message}`);return false}}
function connectEvents(){if(eventSource)eventSource.close();eventSource=new EventSource('/api/swing/events');const accept=reset=>event=>{let payload;try{payload=JSON.parse(event.data)}catch(error){markFailure('sse',`事件数据无效：${error.message}`);return}applySsePayload(payload,reset).catch(error=>markFailure('sse',`事件数据无效：${error.message}`))};eventSource.addEventListener('snapshot',accept(false));eventSource.addEventListener('delta',accept(false));eventSource.addEventListener('reset',accept(true));eventSource.onopen=()=>{eventStreamOpened(state);liveNode.textContent='事件连接已建立，等待有效快照'};eventSource.onerror=()=>markFailure('sse','波段事件连接中断，执行已暂停')}
function formPayload(form){return Object.fromEntries(new FormData(form).entries())}
function resultNode(form){return form.querySelector('.form-result')}
function showFormResult(form,message,ok){const node=resultNode(form);node.textContent=message;node.className=`form-result ${ok?'success':'error'}`}
function initialPositionRows(){return [...document.querySelectorAll('[data-initial-position]')].map(row=>({symbol:row.dataset.initialPosition,enabled:row.querySelector('[data-position-enabled]').checked,shares:row.querySelector('[data-position-shares]').value,average_cost:row.querySelector('[data-position-cost]').value,planned_risk_per_share:row.querySelector('[data-position-risk]').value}))}
document.querySelector('#portfolio-initialize-form').addEventListener('submit',async event=>{event.preventDefault();const form=event.currentTarget,button=form.querySelector('[type="submit"]'),values=formPayload(form);let payload;try{const cash=finite(values.cash),risk=values.default_risk_per_trade===''?null:finite(values.default_risk_per_trade);if(cash==null||cash<0)throw new Error('初始现金无效');payload={name:values.name,cash,initial_positions:buildInitialPositions(initialPositionRows())};if(risk!=null)payload.default_risk_per_trade=risk}catch(error){showFormResult(form,error.message,false);return}button.disabled=true;try{await submitIntent(form.id,'/api/swing/portfolio/initialize',payload);showFormResult(form,'账户初始化记录已保存',true);await loadSnapshot(false)}catch(error){showFormResult(form,error.message,false)}finally{button.disabled=false}});
document.querySelector('#trade-form').addEventListener('submit',async event=>{event.preventDefault();const form=event.currentTarget,button=form.querySelector('[type="submit"]'),values=formPayload(form),local=new Date(values.executed_at);if(Number.isNaN(local.getTime())){showFormResult(form,'成交时间无效',false);return}const payload={symbol:values.symbol,side:values.side,shares:Number(values.shares),price:Number(values.price),fee:Number(values.fee),executed_at:local.toISOString()};if(values.planned_risk_per_share)payload.planned_risk_per_share=Number(values.planned_risk_per_share);if(values.exit_reason)payload.exit_reason=values.exit_reason;button.disabled=true;try{await submitIntent(form.id,'/api/swing/trades',payload);showFormResult(form,'成交记录已保存',true);await loadSnapshot(false)}catch(error){showFormResult(form,error.message,false)}finally{button.disabled=false}});
document.querySelector('#reverse-form').addEventListener('submit',async event=>{event.preventDefault();const form=event.currentTarget,button=form.querySelector('[type="submit"]'),id=String(new FormData(form).get('event_id')||'');if(!validEventId(id)){showFormResult(form,'成交事件 UUID 格式无效',false);return}button.disabled=true;try{await submitIntent(form.id,`/api/swing/trades/${encodeURIComponent(id)}/reverse`,{});showFormResult(form,'冲正记录已保存',true);await loadSnapshot(false)}catch(error){showFormResult(form,error.message,false)}finally{button.disabled=false}});
async function transitionAlert(button){const id=button.dataset.alertId,action=button.dataset.alertAction,node=button.closest('.alert-item')?.querySelector('.form-result');if(!canActOnAlert(state,button.dataset.alertScope,button.dataset.alertState)){if(node){node.textContent='安全暂停期间临时提醒不可操作';node.className='form-result error'}return}if(!validAlertId(id)||!['acknowledge','ignore'].includes(action)){if(node)node.textContent='提醒标识无效';return}button.disabled=true;try{await submitIntent(`${action}:${id}`,`/api/swing/alerts/${encodeURIComponent(id)}/${encodeURIComponent(action)}`,{});if(node){node.textContent=action==='acknowledge'?'已标记为已读':'已忽略本次';node.className='form-result success'}await loadSnapshot(false)}catch(error){if(node){node.textContent=error.message;node.className='form-result error'}}finally{button.disabled=false}}
function openTradeForAlert(symbol,scope,alertState){if(!canActOnAlert(state,scope,alertState)||!validSymbol(symbol))return;selectSymbol(symbol);const tab=document.querySelector('[data-panel="ledger-panel"]');if(tab)tab.click();document.querySelector('#trade-form [name="symbol"]').value=symbol;document.querySelector('#trade-form [name="shares"]').focus()}
permissionButton.addEventListener('click',async()=>{notificationOptIn=true;if(!('Notification'in window)){permissionButton.textContent='此浏览器不支持通知';renderHealth();return}try{const permission=await Notification.requestPermission();permissionButton.textContent=permission==='granted'?'通知已授权':permission==='denied'?'通知已拒绝':'通知未授权';renderHealth()}catch(error){setError(`通知授权失败：${error.message}`)}});
const tabs=[...document.querySelectorAll('[role="tab"]')];function activateTab(tab,{focus=false}={}){for(const current of tabs){const active=current===tab;current.classList.toggle('active',active);current.setAttribute('aria-selected',String(active));current.tabIndex=active?0:-1;document.querySelector('#'+current.dataset.panel).hidden=!active}if(focus)tab.focus()}for(const tab of tabs){tab.addEventListener('click',()=>activateTab(tab));tab.addEventListener('keydown',event=>{const index=tabs.indexOf(tab);let next=null;if(event.key==='ArrowRight')next=(index+1)%tabs.length;else if(event.key==='ArrowLeft')next=(index-1+tabs.length)%tabs.length;else if(event.key==='Home')next=0;else if(event.key==='End')next=tabs.length-1;if(next!=null){event.preventDefault();activateTab(tabs[next],{focus:true})}})}
const tradeSymbol=document.querySelector('#trade-form [name="symbol"]'),initialRows=document.querySelector('#initial-position-rows');for(const [symbol,name] of VERIFIED_SYMBOLS){const option=document.createElement('option');option.value=symbol;option.textContent=`${name} ${symbol}`;tradeSymbol.append(option);const row=document.createElement('div');row.dataset.initialPosition=symbol;row.innerHTML=`<label class="toggle-label"><input data-position-enabled type="checkbox">${escapeHtml(name)} ${symbol}</label><label class="field"><span>份额</span><input data-position-shares type="number" min="100" step="100" disabled></label><label class="field"><span>平均成本</span><input data-position-cost type="number" min="0.001" step="0.001" disabled></label><label class="field"><span>每份计划风险</span><input data-position-risk type="number" min="0" step="0.001" value="0" disabled></label>`;const enabled=row.querySelector('[data-position-enabled]'),inputs=[...row.querySelectorAll('input:not([data-position-enabled])')];enabled.addEventListener('change',()=>{for(const input of inputs)input.disabled=!enabled.checked});initialRows.append(row)}
loadSnapshot(true).finally(connectEvents);
</script>
</body>
</html>
"""


__all__ = ["SWING_PAGE"]
