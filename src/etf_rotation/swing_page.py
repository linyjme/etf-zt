from __future__ import annotations


SWING_PAGE = r"""<!doctype html>
<html lang="zh-CN">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>指数ETF波段监控</title>
<style>
.quality-summary{margin-top:20px;padding-top:16px;border-top:1px solid #293d58}.quality-summary li{overflow-wrap:anywhere}.quality-summary summary{color:#91a3bb;cursor:pointer}
.selected-holding{margin-top:14px;border:1px solid #344a65;border-radius:9px;background:#0b1929;padding:10px 12px}.holding-heading{display:flex;justify-content:space-between;align-items:center;gap:12px;flex-wrap:wrap}.holding-heading h3{font-size:14px}.holding-heading a{font-size:12px}.holding-facts{display:flex;gap:10px 24px;flex-wrap:wrap;margin:8px 0;font-variant-numeric:tabular-nums}.holding-facts dt{font-size:11px;color:var(--muted)}.holding-facts dd{margin:0;font-size:15px}.holding-context{font-size:12px;color:var(--muted);margin:0;overflow-wrap:anywhere}.card.execution-notice{padding:10px 14px}.execution-notice[hidden]{display:none}.execution-notice.notice-info{border-color:#344f70;background:#102238;color:#b9cde4}.execution-notice.notice-warning{border-color:#79622d;background:#2b2415;color:#f2d089}.execution-notice.notice-danger{border-color:#91454e;background:#371c27;color:#ffb0b7}.notice-summary{margin:0;font-weight:600}.execution-notice details{margin-top:5px;font-size:12px}.execution-notice summary{cursor:pointer}.execution-notice details p{margin:6px 0 0;overflow-wrap:anywhere}.status-pill.danger{border-color:var(--red);color:#ffb0b7}
:root{color-scheme:dark;--bg:#07111f;--panel:#101d30;--panel2:#0a1626;--line:#293d58;--text:#e9f0fa;--muted:#91a3bb;--blue:#65a8ff;--green:#39d69f;--yellow:#f2bd4e;--red:#ff6570;--purple:#b88cff}
*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at top,#152945,var(--bg) 46%);color:var(--text);font:14px/1.5 system-ui,"Microsoft YaHei",sans-serif}button,input,select{font:inherit}a{color:#acd1ff}.shell{max-width:1480px;margin:auto;padding:22px 18px 36px}nav[aria-label="监控模式"]{display:flex;flex-wrap:wrap;gap:8px;margin-bottom:18px}nav[aria-label="监控模式"] a{border:1px solid var(--line);border-radius:999px;padding:6px 12px;text-decoration:none}nav[aria-label="监控模式"] a[aria-current=page]{border-color:var(--blue);background:#18345a;color:white}.topbar{display:flex;justify-content:space-between;align-items:flex-start;gap:16px;margin-bottom:14px}.topbar h1{font-size:27px;margin:0}.subtitle,.muted,.help{color:var(--muted)}.guard{display:flex;gap:10px;align-items:center;flex-wrap:wrap;justify-content:flex-end}.badge{border:1px solid #42658a;border-radius:99px;padding:5px 10px;color:#b9d9ff}.button{border:1px solid #3c6ca7;border-radius:8px;background:#2366ad;color:white;padding:8px 12px;cursor:pointer}.button.secondary{background:#132842}.button.danger{border-color:#8f4350;background:#542631}.button:focus-visible,a:focus-visible,input:focus-visible,select:focus-visible,[tabindex="0"]:focus-visible{outline:3px solid #91c7ff;outline-offset:2px}.button:disabled{opacity:.55;cursor:not-allowed}.card,.sidebar{border:1px solid var(--line);border-radius:14px;background:linear-gradient(145deg,#12223a,#0c1728);box-shadow:0 12px 38px #0004}.layout{display:grid;grid-template-columns:285px minmax(0,1fr);gap:16px;align-items:start}.sidebar{position:sticky;top:12px;padding:14px}.sidebar h2,.card h2,.card h3{margin:0}.watch-help{margin:6px 0 12px;font-size:12px;color:var(--muted)}.watchlist{display:grid;gap:7px}.watch-row{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:8px;align-items:center;border:1px solid transparent;border-radius:9px;background:#081421;padding:8px}.watch-row.selected{border-color:var(--blue)}.watch-select{border:0;background:transparent;color:var(--text);padding:0;text-align:left;cursor:pointer;min-width:0}.watch-select strong,.watch-select span{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.watch-select span{color:var(--muted);font-size:11px}.state-dot{display:inline-block;width:8px;height:8px;border-radius:50%;margin-right:6px;background:var(--muted)}.state-dot.red{background:var(--red)}.state-dot.yellow{background:var(--yellow)}.state-dot.green{background:var(--green)}.toggle-label{display:flex;align-items:center;gap:5px;color:var(--muted);font-size:12px}.main-stack{display:grid;gap:14px}.card{padding:16px;min-width:0}.alert-banner{border-color:#91454e;background:#371c27}.alert-banner:empty{display:none}#swing-errors{color:#ff9ca4;margin:8px 0;white-space:pre-wrap}#swing-errors:empty{display:none}#swing-live-status{min-height:1.5em;color:var(--muted)}#swing-live-status.success{color:#7beac8}.detail-head{display:flex;justify-content:space-between;gap:14px;align-items:flex-start}.title-symbol{font-size:12px;color:var(--muted)}.status-pill{border:1px solid var(--line);border-radius:999px;padding:4px 9px}.status-pill.paused{border-color:var(--yellow);color:#ffd774}.status-pill.ready{border-color:var(--green);color:#77edc7}.status-pill.observe{border-color:var(--blue);color:#acd1ff}.chart-layout{display:grid;grid-template-columns:minmax(0,1.65fr) minmax(260px,.7fr);gap:14px;margin-top:14px}.chart-panel{position:relative;min-width:0}.chart-title{display:flex;justify-content:space-between;gap:10px;flex-wrap:wrap}.legend{display:flex;gap:12px;flex-wrap:wrap;color:var(--muted);font-size:12px}.legend i{display:inline-block;width:14px;border-top:2px solid;margin-right:4px;vertical-align:middle}.legend .close{border-color:white}.legend .ma20{border-color:var(--yellow)}.legend .ma60{border-color:var(--blue)}.legend .entry{border-color:var(--green)}.legend .stop{border-color:var(--red)}.chart-svg{display:block;width:100%;height:340px;margin-top:8px;touch-action:none}.axis{stroke:#50647f}.grid{stroke:#273a53;stroke-width:1}.axis-label{fill:#90a3bc;font-size:11px}.close-line,.ma20-line,.ma60-line{fill:none;vector-effect:non-scaling-stroke}.close-line{stroke:white;stroke-width:2}.ma20-line{stroke:var(--yellow);stroke-width:2}.ma60-line{stroke:var(--blue);stroke-width:2}.entry-zone{fill:#39d69f22;stroke:var(--green);stroke-dasharray:5 4}.hard-stop{stroke:var(--red);stroke-width:2;stroke-dasharray:4 4}.trailing-stop{stroke:var(--purple);stroke-width:2;stroke-dasharray:7 4}.chart-cursor{stroke:#9cafc7;stroke-dasharray:3 3;display:none}.chart-tooltip{position:absolute;display:none;pointer-events:none;z-index:4;max-width:260px;border:1px solid #4d6684;border-radius:8px;background:#06101deb;padding:8px 10px;white-space:pre-line;font:12px ui-monospace,monospace}.plan{background:#081421;border-radius:10px;padding:13px}.plan dl{display:grid;grid-template-columns:1fr auto;gap:7px 10px;margin:12px 0}.plan dt{color:var(--muted)}.plan dd{margin:0;text-align:right}.paused-note{color:#ffd774}.evidence{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;margin-top:14px}.evidence div{background:#081421;border-radius:8px;padding:9px;overflow-wrap:anywhere}.evidence span{display:block;color:var(--muted);font-size:11px}.tabs{display:grid;grid-template-columns:repeat(4,auto);justify-content:start;gap:6px;border-bottom:1px solid var(--line);margin-bottom:12px}.tab{border:0;border-bottom:2px solid transparent;background:transparent;color:var(--muted);padding:8px 11px;cursor:pointer}.tab.active{border-color:var(--blue);color:white}.panel[hidden]{display:none}.form-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px}.field{display:grid;gap:4px}.field span{color:var(--muted);font-size:12px}.field input,.field select{width:100%;min-width:0;border:1px solid var(--line);border-radius:7px;background:#071321;color:var(--text);padding:8px}.form-actions{display:flex;gap:8px;align-items:center;margin-top:11px}.form-result{min-height:1.5em}.form-result.error{color:#ff9ca4}.form-result.success{color:#7beac8}.alert-list,.ledger-list{display:grid;gap:8px}.alert-item,.ledger-item{border:1px solid var(--line);border-radius:9px;background:#081421;padding:10px}.alert-actions{display:flex;gap:6px;margin-top:8px}.empty{padding:32px 14px;text-align:center;color:var(--muted)}.health-grid{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:8px}.health-grid div{background:#081421;border-radius:8px;padding:8px}.health-grid span{display:block;color:var(--muted);font-size:11px}
@media(max-width:850px){.layout,.chart-layout{grid-template-columns:1fr}.sidebar{position:static}.watchlist{grid-template-columns:repeat(2,minmax(0,1fr))}.health-grid{grid-template-columns:repeat(3,minmax(0,1fr))}}
@media(max-width:600px){.shell{padding:14px 10px 28px}.topbar,.detail-head{flex-direction:column}.guard{justify-content:flex-start}.watchlist,.evidence{grid-template-columns:repeat(2,minmax(0,1fr))}.health-grid,.form-grid{grid-template-columns:1fr 1fr}.tabs{grid-template-columns:repeat(2,1fr)}.button,.form-actions .button{width:100%}.form-actions{align-items:stretch;flex-direction:column}.chart-svg{height:285px}}
.backtest-toolbar{display:flex;gap:10px;align-items:end;flex-wrap:wrap;margin:8px 0 12px}.backtest-toolbar .field{min-width:190px}.backtest-summary{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px}.backtest-summary div{min-width:0;background:#081421;border-radius:8px;padding:9px;overflow-wrap:anywhere}.backtest-summary span{display:block;color:var(--muted);font-size:11px}.backtest-detail{margin-top:10px;border:1px solid var(--line);border-radius:9px;background:#081421;padding:10px;overflow-wrap:anywhere}.backtest-detail h4{margin:0 0 6px}.backtest-error{border:1px solid #91454e;border-radius:9px;background:#371c27;color:#ffb0b7;padding:12px}.backtest-variants{display:grid;gap:6px;margin-top:10px}.backtest-variants details{border-top:1px solid var(--line);padding:7px 0}.backtest-variants summary{cursor:pointer;overflow-wrap:anywhere}.backtest-folds{display:grid;gap:6px;margin-top:7px}.backtest-folds>div{display:grid;grid-template-columns:.45fr 1fr 1fr 1.2fr;gap:7px;background:#06111f;border-radius:7px;padding:7px;font-size:12px;overflow-wrap:anywhere}
.indicator-summary{margin-top:14px;border-top:1px solid var(--line);padding-top:14px}.indicator-summary header{display:flex;align-items:center;justify-content:space-between;gap:10px;flex-wrap:wrap}.indicator-summary header h3{margin:0}.indicator-status{border:1px solid var(--line);border-radius:999px;padding:3px 8px;color:var(--muted);font-size:12px}.indicator-status.ready{border-color:var(--green);color:#77edc7}.indicator-status.warmup{border-color:var(--yellow);color:#ffd774}.indicator-status.error{border-color:var(--red);color:#ffb0b7}.indicator-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px;margin-top:10px}.indicator-cell{background:#081421;border-radius:8px;padding:9px;overflow-wrap:anywhere}.indicator-cell strong{display:block;font-size:15px;margin-top:2px;font-variant-numeric:tabular-nums}.indicator-cell span{display:block;color:var(--muted);font-size:11px}.indicator-note{margin:9px 0 0;color:var(--muted);font-size:12px}
@media(max-width:600px){.indicator-grid{grid-template-columns:repeat(2,minmax(0,1fr))}}
@media(max-width:850px){.backtest-summary{grid-template-columns:repeat(2,minmax(0,1fr))}}
@media(max-width:600px){.backtest-toolbar{align-items:stretch;flex-direction:column}.backtest-toolbar .field{min-width:0}.backtest-toolbar .button{width:100%}.backtest-summary{grid-template-columns:repeat(2,minmax(0,1fr))}.backtest-folds>div{grid-template-columns:1fr 1fr}}
.valuation-summary{margin-top:14px;border-top:1px solid var(--line);padding-top:14px}.valuation-summary header{display:flex;align-items:center;justify-content:space-between;gap:10px;flex-wrap:wrap}.valuation-summary header h3{margin:0}.valuation-status{border:1px solid var(--line);border-radius:999px;padding:3px 8px;color:var(--muted);font-size:12px}.valuation-status.low{border-color:var(--green);color:#77edc7}.valuation-status.high{border-color:var(--red);color:#ffb0b7}.valuation-status.normal{border-color:var(--yellow);color:#ffd774}.valuation-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px;margin-top:10px}.valuation-cell{background:#081421;border-radius:8px;padding:9px;overflow-wrap:anywhere}.valuation-cell strong{display:block;font-size:15px;margin-top:2px;font-variant-numeric:tabular-nums}.valuation-cell span{display:block;color:var(--muted);font-size:11px}.valuation-note{margin:9px 0 0;color:var(--muted);font-size:12px}.valuation-impact{margin:9px 0 0;padding:8px 10px;border-radius:8px;background:#0b1d31;color:#b9cde4;font-size:12px}@media(max-width:600px){.valuation-grid{grid-template-columns:repeat(2,minmax(0,1fr))}}
.shadow-summary{margin-top:14px;border:1px solid #3f5877;border-radius:10px;background:#0b1929;padding:12px}.shadow-summary header{display:flex;align-items:center;justify-content:space-between;gap:10px;flex-wrap:wrap}.shadow-summary header h3{margin:0}.shadow-status{border:1px solid var(--blue);border-radius:999px;padding:3px 8px;color:#acd1ff;font-size:12px}.shadow-status.blocked{border-color:var(--yellow);color:#ffd774}.shadow-variants{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;margin-top:10px}.shadow-variant{background:#081421;border-radius:8px;padding:9px;overflow-wrap:anywhere}.shadow-variant strong{display:block;font-size:14px}.shadow-variant span{display:block;color:var(--muted);font-size:11px;margin-top:3px}.shadow-note{margin:9px 0 0;color:var(--muted);font-size:12px}.shadow-reasons{margin:8px 0 0;color:#ffd774;font-size:12px}@media(max-width:600px){.shadow-variants{grid-template-columns:1fr}}
</style>
</head>
<body>
<main class="shell">
  <nav aria-label="监控模式"><a href="/portfolio">资产总览</a><a href="/">做T监控</a><a href="/swing" aria-current="page">波段监控</a><a href="/notifications">通知中心</a></nav>
  <header class="topbar"><div><h1>指数ETF波段监控</h1><div class="subtitle">趋势过滤 · 回调计划 · 手工成交确认</div></div><div class="guard"><span class="badge">仅监控，不自动交易</span><button id="notification-permission" class="button secondary" type="button">通知需点击授权</button></div></header>
  <div id="swing-errors" role="alert"></div>
  <div id="swing-live-status" aria-live="polite">正在载入波段状态</div>
  <div class="layout">
    <aside class="sidebar"><h2>波段标的</h2><p class="watch-help">只可启停已核验的ETF；待接入标的仅展示登记信息，不启用策略。</p><nav id="swing-watchlist" class="watchlist" aria-label="波段标的"><div class="empty">正在载入</div></nav></aside>
    <div class="main-stack">
      <section id="intraday-warning" class="card execution-notice notice-warning" role="status"></section>
      <section id="v11-workbench" class="card"><div class="detail-head"><div><h2>今日行动 · SWING_V11_SHADOW</h2><div class="muted">只读影子策略：完成日线 + 周线确认 + 14:45 准收盘，人工确认后再决定是否操作。</div></div><span class="status-pill observe">不支持自动下单</span></div><div id="v11-action-summary" class="evidence"><div class="empty">正在载入 V11 状态</div></div></section>
      <section id="swing-detail" class="card"><div class="empty">正在载入波段状态</div></section>
      <section class="card" aria-label="健康状态"><h2>数据与执行状态</h2><div id="health-grid" class="health-grid"><div><span>服务健康</span>—</div><div><span>日线数据</span>—</div><div><span>行情状态</span>—</div><div><span>策略计算</span>—</div><div><span>组合账本</span>—</div><div><span>浏览器通知</span>—</div></div></section>
      <section class="card"><div class="tabs" role="tablist" aria-label="波段信息"><button id="alerts-tab" class="tab active" data-panel="alerts-panel" role="tab" aria-controls="alerts-panel" aria-selected="true" tabindex="0">提醒记录</button><button id="ledger-tab" class="tab" data-panel="ledger-panel" role="tab" aria-controls="ledger-panel" aria-selected="false" tabindex="-1">成交账本</button><button id="backtest-tab" class="tab" data-panel="backtest-panel" role="tab" aria-controls="backtest-panel" aria-selected="false" tabindex="-1">波段回测</button><button id="params-tab" class="tab" data-panel="params-panel" role="tab" aria-controls="params-panel" aria-selected="false" tabindex="-1">参数说明</button></div>
        <div id="alerts-panel" class="panel" role="tabpanel" aria-labelledby="alerts-tab" tabindex="0"><div id="alert-list" class="alert-list"><div class="empty">暂无提醒</div></div></div>
        <div id="ledger-panel" class="panel" role="tabpanel" aria-labelledby="ledger-tab" tabindex="0" hidden><h3>成交账本</h3><div id="ledger-list" class="ledger-list"><div class="empty">账户尚未初始化</div></div>
          <h3>初始化波段账户</h3><form id="portfolio-initialize-form"><div class="form-grid"><label class="field"><span>账户名称</span><input name="name" required maxlength="40" autocomplete="off"></label><label class="field"><span>初始现金（元）</span><input name="cash" required type="number" min="0" step="0.01"></label><label class="field"><span>单笔风险比例</span><input name="default_risk_per_trade" type="number" min="0.0001" max="1" step="0.0001" placeholder="留空使用服务配置"></label></div><fieldset><legend>已有持仓（可选，按100份整手录入）</legend><div id="initial-position-rows" class="form-grid"></div></fieldset><div class="help">初始化后不可重复。失败后会保留账户和全部持仓输入。</div><div class="form-actions"><button class="button" type="submit">初始化账户</button><span class="form-result" aria-live="polite"></span></div></form>
          <h3>确认实际成交</h3><form id="trade-form"><div class="form-grid"><label class="field"><span>标的</span><select name="symbol" required></select></label><label class="field"><span>方向</span><select name="side"><option value="BUY">买入</option><option value="SELL">卖出</option></select></label><label class="field"><span>份额（整手约束由服务校验）</span><input name="shares" type="number" min="1" step="1" required></label><label class="field"><span>成交价格</span><input name="price" type="number" min="0.001" step="0.001" required></label><label class="field"><span>费用</span><input name="fee" type="number" min="0" step="0.01" required></label><label class="field"><span>成交时间（含本地时区）</span><input name="executed_at" type="datetime-local" required></label><label class="field"><span>每份计划风险</span><input name="planned_risk_per_share" type="number" min="0" step="0.001"></label><label class="field"><span>退出原因（卖出可选）</span><select name="exit_reason"><option value="">无</option><option value="STOP_EXIT">止损退出</option></select></label></div><div class="help">只记录你已经在外部完成的实际成交；失败后会保留已输入内容。</div><div class="form-actions"><button class="button" type="submit">保存成交</button><span class="form-result" aria-live="polite"></span></div></form>
          <h3>冲正成交</h3><form id="reverse-form"><div class="form-grid"><label class="field"><span>成交事件 UUID</span><input name="event_id" required autocomplete="off" pattern="[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}"></label></div><div class="help">仅用于纠正误录，失败后会保留已输入内容。</div><div class="form-actions"><button class="button danger" type="submit">冲正记录</button><span class="form-result" aria-live="polite"></span></div></form>
        </div>
        <div id="backtest-panel" class="panel" role="tabpanel" aria-labelledby="backtest-tab" tabindex="0" hidden><h3>波段回测</h3><p class="help">使用已完成日线进行只读回测；不会写入成交账本，也不会改变正式提醒。</p><div class="backtest-toolbar"><label class="field"><span>回测范围</span><select id="backtest-scope"><option value="portfolio">已启用ETF组合</option><option value="symbol">当前标的</option></select></label><button id="backtest-run" class="button secondary" type="button">重新运行</button><span id="backtest-status" class="form-result" aria-live="polite">打开面板后自动载入</span></div><div id="backtest-result"><div class="empty">尚未载入回测结果</div></div></div>
        <div id="params-panel" class="panel" role="tabpanel" aria-labelledby="params-tab" tabindex="0" hidden><h3>参数说明</h3><p class="muted">指标使用完成日线。价格线为复权收盘（最新交易日基准）；收盘、MA20、MA60 均按最新原始价/复权价比例映射。买入区和止损线保留真实委托价格；当日原始收盘仅在提示框中单独展示。所有候选均需通过数据、日历和组合风险门槛。</p><h3>趋势评分说明</h3><p class="muted">趋势评分 = 60日均线斜率贡献 + 收盘价相对MA60位置贡献；只计入正向贡献，最低为0。它用于衡量中期趋势强弱，不是收益率，也不是单独的买卖信号。</p><table><thead><tr><th>趋势评分</th><th>参考含义</th></tr></thead><tbody><tr><td>0.00</td><td>没有正向趋势贡献，可能是价格低于MA60或MA60未上行</td></tr><tr><td>0.00–0.50</td><td>趋势支撑较弱</td></tr><tr><td>0.50–1.50</td><td>有一定趋势改善，但强度一般</td></tr><tr><td>1.50–2.00</td><td>中期趋势相对较强</td></tr><tr><td>2.00以上</td><td>正向趋势条件较强，仍需结合波段状态、成交量和风控判断</td></tr></tbody></table><p class="muted">注意：DATA_UNAVAILABLE 或 DATA_NOT_READY 时，评分不具备参考意义，应以“—”表示。</p></div>
      </section>
    </div>
  </div>
</main>
<script>
/* SWING_PAGE_HELPERS_START */
function createPageState(){return {snapshot:null,snapshotRevision:-1,auxiliary:null,dailyBySymbol:new Map(),dailyRevisions:new Map(),selectedSymbol:null,safetyPaused:true,selectedExecutable:false,executionPaused:true,lastFormalPlan:null,lastFormalPlans:new Map(),failureSources:new Set(),failureMessages:new Map(),streamConnected:false}}
function cloneJson(value){return value==null?value:JSON.parse(JSON.stringify(value))}
function validSymbol(value){return typeof value==='string'&&/^[0-9]{6}$/.test(value)}
function validAlertId(value){return typeof value==='string'&&/^[0-9a-f]{24}$/.test(value)}
function validEventId(value){return typeof value==='string'&&/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/.test(value)}
function escapeHtml(value){return String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]))}
function finite(value){if(typeof value==='number')return Number.isFinite(value)?value:null;if(typeof value!=='string'||value===''||value.trim()!==value||!/^[+-]?(?:\d+\.?\d*|\.\d+)(?:[eE][+-]?\d+)?$/.test(value))return null;const number=Number(value);return Number.isFinite(number)?number:null}
function validDate(value){return typeof value==='string'&&/^\d{4}-\d{2}-\d{2}$/.test(value)&&!Number.isNaN(Date.parse(value+'T00:00:00Z'))}
function applyDailyPayload(state,payload){
  if(!payload||!validSymbol(payload.symbol))throw new Error('日线标的无效');
  const revision=Number(payload.revision);if(!Number.isInteger(revision)||revision<0)throw new Error('日线版本无效');
  if(!Array.isArray(payload.upserts))throw new Error('日线增量无效');
  const prior=Number(state.dailyRevisions.get(payload.symbol)??-1);
  if(!payload.reset&&revision<prior)return false;
  const bars=payload.reset?new Map():new Map(state.dailyBySymbol.get(payload.symbol)||[]);
  for(const raw of payload.upserts){if(!raw||!validDate(raw.trading_date)||raw.symbol&&raw.symbol!==payload.symbol)throw new Error('日线记录无效');bars.set(raw.trading_date,cloneJson(raw))}
  state.dailyBySymbol.set(payload.symbol,bars);state.dailyRevisions.set(payload.symbol,revision);return true;
}
function applySnapshotPayload(state,payload,authoritativeReset=false){
  if(!payload||!Array.isArray(payload.items))throw new Error('波段快照无效');
  const revision=Number(payload.revision);if(!Number.isInteger(revision)||revision<0)throw new Error('快照版本无效');
  if(!authoritativeReset&&revision<state.snapshotRevision)return false;
  for(const item of payload.items){if(!item||!validSymbol(item.symbol))throw new Error('快照标的无效')}
  const selected=state.selectedSymbol,incoming=cloneJson(payload);if(state.failureSources.size||!payloadSafetyReady(incoming))stripIntradaySnapshot(incoming);state.snapshot=incoming;state.snapshotRevision=revision;
  if(selected&&payload.items.some(item=>item.symbol===selected))state.selectedSymbol=selected;
  else if(!selected||!payload.items.some(item=>item.symbol===selected))state.selectedSymbol=payload.items[0]?.symbol||selected||null;
  for(const item of payload.items){if(item.formal_decision){const plan={symbol:item.symbol,as_of_trading_date:item.signal_data_date||payload.as_of_trading_date||null,formal_state:item.formal_state,formal_decision:cloneJson(item.formal_decision)};state.lastFormalPlans.set(item.symbol,plan);if(item.symbol===state.selectedSymbol)state.lastFormalPlan=plan}}
  refreshExecutionPaused(state);
  return true;
}
function stripIntradaySnapshot(snapshot){if(!snapshot)return snapshot;for(const item of snapshot.items||[])item.intraday_overlay=null;const overlayStates=new Set(['APPROACHING_ENTRY_ZONE','PREDEFINED_STOP_TOUCHED']);snapshot.active_alerts=(snapshot.active_alerts||[]).filter(alert=>alert?.scope!=='INTRADAY'&&!overlayStates.has(alert?.state));snapshot.alerts=(snapshot.alerts||[]).filter(alert=>alert?.scope!=='INTRADAY'&&!overlayStates.has(alert?.state));return snapshot}
function revokeIntraday(state,reason){
  state.safetyPaused=true;state.selectedExecutable=false;state.executionPaused=true;
  if(!state.snapshot)return;
  const selected=state.selectedSymbol||state.snapshot.items?.[0]?.symbol;
  const selectedItem=(state.snapshot.items||[]).find(item=>item.symbol===selected);
  if(selectedItem)state.lastFormalPlan={symbol:selectedItem.symbol,as_of_trading_date:selectedItem.signal_data_date||state.snapshot.as_of_trading_date||null,formal_state:selectedItem.formal_state,formal_decision:cloneJson(selectedItem.formal_decision||{}),paused_reason:reason};
  stripIntradaySnapshot(state.snapshot);
}
function shouldNotifyAlert(alert,userOptedIn,permission,seen,safetyPaused=false){return Boolean(!safetyPaused&&userOptedIn&&permission==='granted'&&alert&&alert.alert_id&&!seen.has(alert.alert_id)&&!alert.retracted&&(alert.currently_active===true||alert.active===true||alert.active_notification===true))}
const REQUIRED_EXECUTION_HEALTH=Object.freeze({service:'OK',configuration:'OK',daily:'OK',portfolio:'OK',alerts:'OK',intraday:'REALTIME'});
function holdingsSnapshotPresent(payload){return Boolean(payload?.holdings_snapshot&&payload.holdings_snapshot.status!=='ABSENT')}
function watchActivationAllowed(payload,symbol){return (payload?.available_symbols||[]).find(item=>item.symbol===symbol)?.can_enable!==false}
function historyPreparationCopy(payload,symbol){
  const info=(payload?.available_symbols||[]).find(item=>item.symbol===symbol)||{};
  const count=Number.isInteger(info.daily_count)&&info.daily_count>=0?info.daily_count:null;
  const minimum=Number.isInteger(info.minimum_daily_bars)&&info.minimum_daily_bars>0?info.minimum_daily_bars:null;
  if(info.can_enable===true)return {label:`日线 ${count??'—'} 日 · 可启用`,reason:`最新完成日 ${info.latest_daily_date||'—'}；启用后查看趋势，仍须通过提醒安全门槛`};
  return {label:`日线 ${count??'—'}/${minimum??'—'} · 未就绪`,reason:info.enable_block_reason||'先独立补齐并校验日线，再启用监控'};
}
function selectedHoldingMarkup(payload,symbol){
  const view=payload?.holdings_snapshot,snapshot=view?.status==='SNAPSHOT_ONLY'?view.snapshot:null,position=(snapshot?.positions||[]).find(item=>item.symbol===symbol),code=validSymbol(symbol)?symbol:'—';
  const note=view?.status==='INVALID'?'持仓快照待核对；数据未知，不代表空仓。':position?'仅为报告持仓，买入日期和止损未知。':'该标的未登记持仓报告；— 不代表空仓。';
  const field=(label,key,digits)=>`<div><dt>${label}</dt><dd data-holding-field="${key}">${backtestNumber(position?.[key],digits)}</dd></div>`;
  return `<aside class="selected-holding" aria-label="当前标的持仓"><div class="holding-heading"><h3>当前标的持仓 · ${escapeHtml(position?.name||code)}</h3><a href="/portfolio">查看资产总览 →</a></div><dl class="holding-facts">${field('报告份额','shares',0)}${field('报告可卖','sellable_shares',0)}${field('报告成本','average_cost',3)}</dl><p class="holding-context">持仓截图时点：${escapeHtml(snapshot?.positions_as_of||'未知')} · 非实时估值。${escapeHtml(note)}${position?.management_mode==='LONG_TERM_ONLY'?' 长期仅记账。':''}</p></aside>`;
}
function payloadSafetyReady(payload){const h=payload?.health||{};return !holdingsSnapshotPresent(payload)&&Object.entries(REQUIRED_EXECUTION_HEALTH).every(([key,value])=>h[key]===value)&&Object.keys(payload?.errors||{}).length===0}
function snapshotExecutionReady(state){const item=(state.snapshot?.items||[]).find(value=>value.symbol===state.selectedSymbol);return Boolean(item&&item.execution_status==='READY_TO_EXECUTE')}
function refreshExecutionPaused(state){state.safetyPaused=state.failureSources.size>0||!payloadSafetyReady(state.snapshot);state.selectedExecutable=!state.safetyPaused&&snapshotExecutionReady(state);state.executionPaused=!state.selectedExecutable;if(state.safetyPaused&&state.snapshot)stripIntradaySnapshot(state.snapshot);return state.executionPaused}
function executionPauseReasons(state){
  const snapshot=state?.snapshot,health=snapshot?.health||{},reasons=new Map();
  const marketCopy=new Map([
    ['REALTIME','盘中行情异常，盘中提醒暂停'],['DELAYED','行情延迟，盘中提醒暂停'],
    ['OUTAGE','行情断流，盘中提醒暂停'],['UNAVAILABLE','暂无实时行情，盘中提醒暂停'],
    ['DATA_ERROR','数据校验异常，盘中提醒暂停'],
    ['LUNCH_BREAK','当前午休，盘中提醒暂停'],['CLOSED','当前已收盘，盘中提醒暂停'],
    ['STALE','行情已过期，盘中提醒暂停'],
  ]);
  const portfolioCopy=holdingsSnapshotPresent(snapshot)?'持仓快照仅记录，需迁移后启用仓位提醒':health.portfolio==='UNINITIALIZED'?'账户未初始化，仓位提醒暂停':
    ['OK','BLOCKED'].includes(health.portfolio)?'组合账本异常，仓位提醒暂停':'组合账本状态未知，仓位提醒暂停';
  const componentCopy=new Map([
    ['service','监控服务异常或未就绪'],['configuration','策略配置异常或未就绪'],
    ['daily','日线数据异常或未就绪'],['portfolio',portfolioCopy],
    ['alerts','提醒服务异常或未就绪'],['calendar','交易日历异常或未就绪'],
    ['intraday',marketCopy.get(health.intraday)||'实时行情状态未知，盘中提醒暂停'],
  ]);
  if(!snapshot)reasons.set('snapshot','波段状态同步中');
  else{
    if(holdingsSnapshotPresent(snapshot))reasons.set('portfolio',portfolioCopy);
    for(const [key,required] of Object.entries(REQUIRED_EXECUTION_HEALTH)){
      if(health[key]!==required)reasons.set(key,componentCopy.get(key));
    }
    for(const key of Object.keys(snapshot.errors||{})){
      const component=componentCopy.has(key)?key:'backend-other';
      if(!reasons.has(component))reasons.set(component,componentCopy.get(key)||'后台状态异常');
    }
  }
  const sourceCopy=new Map([['snapshot','波段快照同步失败'],['sse','实时事件连接或同步中断'],['daily','日线同步失败']]);
  for(const source of state?.failureSources||[]){
    reasons.set(sourceCopy.has(source)?`frontend:${source}`:'frontend-other',sourceCopy.get(source)||'页面数据同步异常');
  }
  return [...new Set(reasons.values())];
}
function executionStatusCopy(state){return state.safetyPaused?`暂停执行：${executionPauseReasons(state).join('；')||'执行状态待确认'}`:state.selectedExecutable?'执行候选已就绪，仍需手工确认':'数据正常，当前仅观察/无执行候选'}
function executionWarningCopy(state){
  if(!state.safetyPaused)return state.selectedExecutable?'':`${executionStatusCopy(state)}。`;
  const date=state.lastFormalPlan?.as_of_trading_date||state.snapshot?.as_of_trading_date;
  const retained=holdingsSnapshotPresent(state.snapshot)?'组合候选及盘中操作提醒已停用；仅保留完成日线趋势供查看。':state.snapshot?`已撤销盘中临时提醒；${validDate(date)?`保留 ${date} 的最后正式计划供查看`:'保留最后正式计划供查看（日期待同步）'}。`:'盘中提醒暂不可用，正在同步正式计划。';
  const guidance=holdingsSnapshotPresent(state.snapshot)?'持仓快照的买入日期、止损及风险未知；仅记录，不激活策略。账户初始化、成交录入和冲正已停用，需完成明确迁移后再启用。':state.snapshot?.health?.portfolio==='UNINITIALIZED'?'请在“成交账本 → 初始化波段账户”填写真实账户信息；仅用于本地手工记账，不自动交易，也不会自动初始化或填入示例金额。':'';
  return `${executionStatusCopy(state)}。${retained}${guidance}`;
}
function executionNotice(state){
  const payload=state?.snapshot,h=payload?.health||{},errors=payload?.errors||{},snapshotOnly=payload?.holdings_snapshot?.status==='SNAPSHOT_ONLY'&&payload.holdings_snapshot.snapshot;
  const faults=[],addFault=copy=>{if(!faults.includes(copy))faults.push(copy)};
  if(state?.failureSources?.size)addFault(state.failureSources.has('sse')?'事件连接中断，执行暂停':'页面数据同步失败，执行暂停');
  if(h.service&&!['OK','STARTING','UNKNOWN'].includes(h.service)&&!(h.service==='BLOCKED'&&h.configuration!=='OK'))addFault('监控服务异常，执行暂停');
  const marketFault={DATA_ERROR:'数据校验异常，盘中提醒暂停',OUTAGE:'行情断流，盘中提醒暂停',STALE:'行情已过期，盘中提醒暂停',DELAYED:'行情延迟，盘中提醒暂停',BLOCKED:'行情异常，盘中提醒暂停'};
  if(marketFault[h.intraday])addFault(marketFault[h.intraday]);
  if(holdingsSnapshotPresent(payload)&&!snapshotOnly)addFault('持仓快照待核对，仓位策略停用');
  for(const [key,label] of [['daily','日线数据'],['alerts','提醒服务'],['calendar','交易日历']])if(h[key]==='BLOCKED')addFault(`${label}异常，执行暂停`);
  if(h.portfolio==='BLOCKED'&&!holdingsSnapshotPresent(payload))addFault('组合账本异常，仓位提醒暂停');
  const errorCopy={service:'监控服务异常，执行暂停',configuration:'策略配置异常，执行暂停',daily:'日线数据异常，执行暂停',portfolio:'组合账本异常，仓位提醒暂停',alerts:'提醒服务异常，执行暂停',calendar:'交易日历异常，执行暂停',intraday:'行情异常，盘中提醒暂停'};
  for(const key of Object.keys(errors)){
    const expected=key==='portfolio'&&(h.portfolio==='UNINITIALIZED'||snapshotOnly&&h.portfolio==='SNAPSHOT_ONLY')||key==='configuration'&&h.configuration!=='OK'||key==='intraday'&&['UNAVAILABLE','UNKNOWN','CLOSED','LUNCH_BREAK'].includes(h.intraday)||key==='daily'&&['UNAVAILABLE','UNKNOWN'].includes(h.daily);
    if(!expected)addFault(errorCopy[key]||'后台状态异常，执行暂停');
  }
  const details=executionWarningCopy(state);
  if(faults.length)return {tone:'danger',summary:faults[0],details};
  if(!payload)return {tone:'warning',summary:'波段状态同步中',details};
  if(h.configuration!=='OK')return {tone:'warning',summary:'策略配置待确认，执行暂停',details};
  if(h.portfolio==='UNINITIALIZED')return {tone:'warning',summary:'账户未初始化，仓位提醒暂停',details};
  if(h.daily!=='OK')return {tone:'warning',summary:'等待已完成日线，执行暂停',details};
  if(!['REALTIME','CLOSED','LUNCH_BREAK'].includes(h.intraday))return {tone:'warning',summary:'等待实时行情，盘中提醒暂停',details};
  if(Object.entries(REQUIRED_EXECUTION_HEALTH).some(([key,value])=>!['portfolio','intraday'].includes(key)&&h[key]!==value)||!snapshotOnly&&h.portfolio!=='OK')return {tone:'warning',summary:'执行条件尚未就绪',details};
  if(snapshotOnly)return {tone:'info',summary:'持仓仅记录，仓位策略待迁移',details};
  if(h.intraday==='CLOSED'||h.intraday==='LUNCH_BREAK')return {tone:'info',summary:h.intraday==='CLOSED'?'当前已收盘，盘中提醒暂停':'当前午休，盘中提醒暂停',details};
  return state.selectedExecutable?{tone:'none',summary:'',details:''}:{tone:'info',summary:'数据正常，当前仅观察',details};
}
function executionWarningMarkup(state){const notice=executionNotice(state);return notice.tone==='none'?'':`<p class="notice-summary">${escapeHtml(notice.summary)}</p>${state.safetyPaused?`<details><summary>查看暂停原因</summary><p>${escapeHtml(notice.details)}</p></details>`:''}`}
function eventStreamOpened(state){state.streamConnected=true;return refreshExecutionPaused(state)}
function acceptSseSnapshot(state,payload,reset){const revision=Number(payload?.revision);if(!Number.isInteger(revision)||revision<0||!reset&&revision<state.snapshotRevision)return false;if(payloadSafetyReady(payload)){state.failureSources.delete('sse');state.failureSources.delete('snapshot');state.failureMessages.delete('sse');state.failureMessages.delete('snapshot')}if(reset){state.dailyBySymbol.clear();state.dailyRevisions.clear()}if(!applySnapshotPayload(state,payload,reset))return false;refreshExecutionPaused(state);return true}
function validateReadModelBundle(state,payload,bundle,authoritativeReset=false){if(!payload||!Array.isArray(payload.items))throw new Error('波段快照无效');const revision=Number(payload.revision);if(!Number.isInteger(revision)||revision<0)throw new Error('快照版本无效');if(!authoritativeReset&&revision<state.snapshotRevision)return false;for(const item of payload.items)if(!item||!validSymbol(item.symbol))throw new Error('快照标的无效');if(!bundle||!bundle.watch||!bundle.portfolio||!bundle.alerts)throw new Error('波段读模型不完整');for(const view of [bundle.watch,bundle.portfolio,bundle.alerts])if(Number(view.revision)!==revision)throw new Error('波段读模型与快照版本不一致');return true}
function alertIsActive(item){if(!item||item.retracted)return false;if(typeof item.currently_active==='boolean')return item.currently_active;return item.active===true||item.active_notification===true}
function normalizeAlertsView(items,historyLimit=80){const maximum=Number.isInteger(historyLimit)&&historyLimit>=0?historyLimit:80,deduped=new Map();for(const item of Array.isArray(items)?items:[]){if(!item||typeof item.alert_id!=='string'||!item.alert_id)continue;const existing=deduped.get(item.alert_id);if(existing&&alertIsActive(existing)&&!alertIsActive(item))continue;if(existing)deduped.delete(item.alert_id);deduped.set(item.alert_id,item)}const values=[...deduped.values()],active=values.filter(alertIsActive),history=values.filter(item=>!alertIsActive(item));return active.concat(history.slice(-maximum)).map(cloneJson)}
function applyReadModelBundle(state,payload,bundle,authoritativeReset=false){if(!validateReadModelBundle(state,payload,bundle,authoritativeReset))return false;const boundedAlertPayload={...bundle.alerts,items:normalizeAlertsView(bundle.alerts.items,80)},auxiliary={watch:cloneJson(bundle.watch),portfolio:cloneJson(bundle.portfolio),alerts:cloneJson(boundedAlertPayload)};if(!applySnapshotPayload(state,payload,authoritativeReset))return false;state.auxiliary=auxiliary;return true}
function boundedAlerts(items,limit=80){return normalizeAlertsView(items,limit)}
function isTemporaryIntradayAlert(scope,alertState){return scope==='INTRADAY'||alertState==='APPROACHING_ENTRY_ZONE'||alertState==='PREDEFINED_STOP_TOUCHED'}
function alertsForDisplay(items,state){const source=Array.isArray(items)?items.filter(Boolean):[];if(!state?.safetyPaused&&!state?.failureSources?.size)return source;return source.filter(alert=>!isTemporaryIntradayAlert(alert.scope,alert.state))}
function canActOnAlert(state,scope,alertState){return !holdingsSnapshotPresent(state?.snapshot)&&!((state?.safetyPaused||state?.failureSources?.size)&&isTemporaryIntradayAlert(scope,alertState))}
function pruneRememberedAlertIds(seen,activeIds,limit=256){const maximum=Number.isInteger(limit)&&limit>0?limit:256,keep=[],added=new Set();for(const id of activeIds||[]){if(seen.has(id)&&!added.has(id)&&keep.length<maximum){keep.push(id);added.add(id)}}for(const id of [...seen].reverse()){if(!added.has(id)&&keep.length<maximum){keep.push(id);added.add(id)}}seen.clear();for(const id of keep.reverse())seen.add(id);return seen}
function rollingAdjusted(bars,windowSize,index){if(index+1<windowSize)return null;let sum=0;for(let i=index-windowSize+1;i<=index;i++){const value=finite(bars[i].adjusted_close);if(value==null)return null;sum+=value}return sum/windowSize}
function chartSeries(source){const bars=[...source].filter(bar=>validDate(bar.trading_date)&&finite(bar.close)!=null&&finite(bar.adjusted_close)!=null).sort((a,b)=>a.trading_date.localeCompare(b.trading_date));if(!bars.length)return {points:[],scale:null};const latest=bars[bars.length-1],scale=latest.close/latest.adjusted_close;if(!Number.isFinite(scale)||scale<=0)return {points:[],scale:null};const all=bars.map((bar,index)=>({date:bar.trading_date,close:finite(bar.adjusted_close)*scale,rawClose:finite(bar.close),ma20:(rollingAdjusted(bars,20,index)??NaN)*scale,ma60:(rollingAdjusted(bars,60,index)??NaN)*scale}));return {points:all.slice(-120),scale}}
function stableJson(value){if(Array.isArray(value))return '['+value.map(stableJson).join(',')+']';if(value&&typeof value==='object')return '{'+Object.keys(value).sort().map(key=>JSON.stringify(key)+':'+stableJson(value[key])).join(',')+'}';return JSON.stringify(value)}
function prepareIntent(registry,owner,path,payload,keyFactory=()=>crypto.randomUUID()){const fingerprint=path+'\n'+stableJson(payload),existing=registry.get(owner);if(existing&&existing.fingerprint===fingerprint)return existing;const record={fingerprint,key:keyFactory(),promise:null};registry.set(owner,record);return record}
function settleIntentSuccess(registry,owner,key){if(registry.get(owner)?.key===key)registry.delete(owner)}
function buildInitialPositions(rows){const result={};for(const row of rows){if(!row.enabled)continue;if(!validSymbol(row.symbol))throw new Error('已有持仓标的无效');const shares=finite(row.shares),cost=finite(row.average_cost),risk=row.planned_risk_per_share===''?0:finite(row.planned_risk_per_share);if(!Number.isInteger(shares)||shares<=0||shares%100!==0)throw new Error(`${row.symbol}份额必须是100的正整数倍`);if(cost==null||cost<=0)throw new Error(`${row.symbol}平均成本必须大于0`);if(risk==null||risk<0)throw new Error(`${row.symbol}每份计划风险不得为负`);result[row.symbol]={shares,average_cost:cost,planned_risk_per_share:risk}}return result}
function pointerIndex(clientX,plotLeft,plotWidth,count){if(!Number.isFinite(clientX)||!Number.isFinite(plotLeft)||!Number.isFinite(plotWidth)||plotWidth<=0||!Number.isInteger(count)||count<=0)return 0;const ratio=Math.max(0,Math.min(1,(clientX-plotLeft)/plotWidth));return Math.round(ratio*Math.max(0,count-1))}
function backtestQuery(scope,symbol){if(scope==='portfolio')return '/api/swing/backtest?scope=portfolio';if(scope==='symbol'&&validSymbol(symbol))return `/api/swing/backtest?scope=symbol&symbol=${encodeURIComponent(symbol)}`;throw new Error('回测范围或标的无效')}
function backtestNumber(value,digits=2){const number=finite(value);return number==null?'—':Number(number).toLocaleString('zh-CN',{minimumFractionDigits:digits,maximumFractionDigits:digits})}
function backtestPercent(value){const number=finite(value);return number==null?'—':(number*100).toFixed(2)+'%'}
function backtestCacheKey(scope,symbol,state){const daily=[...(state?.dailyBySymbol||new Map()).entries()].sort((a,b)=>String(a[0]).localeCompare(String(b[0]))).map(([itemSymbol,bars])=>[itemSymbol,[...(bars?.values?.()||[])].sort((a,b)=>String(a.trading_date).localeCompare(String(b.trading_date))).map(bar=>({trading_date:bar.trading_date,open:bar.open,high:bar.high,low:bar.low,close:bar.close,previous_close:bar.previous_close,adjusted_open:bar.adjusted_open,adjusted_high:bar.adjusted_high,adjusted_low:bar.adjusted_low,adjusted_close:bar.adjusted_close,volume:bar.volume,amount:bar.amount}))]),formal=(state?.snapshot?.items||[]).map(item=>({symbol:item.symbol,formal_state:item.formal_state,signal_data_date:item.signal_data_date,formal_decision:item.formal_decision})).sort((a,b)=>String(a.symbol).localeCompare(String(b.symbol))),watch=(state?.auxiliary?.watch?.items||state?.snapshot?.watchlist?.items||[]).map(item=>({symbol:item.symbol,enabled:item.enabled===true})).sort((a,b)=>String(a.symbol).localeCompare(String(b.symbol))),historyDigest=typeof state?.snapshot?.daily_history_digest==='string'?state.snapshot.daily_history_digest:'';return [scope,scope==='symbol'?symbol:'',state?.snapshot?.as_of_trading_date||'',state?.snapshot?.strategy||'',historyDigest,stableJson(formal),stableJson(watch),stableJson(daily)].join('|')}
function backtestResponseCurrent(requestToken,activeToken,requestKey,currentKey){return Number.isInteger(requestToken)&&requestToken===activeToken&&requestKey===currentKey}
function validBacktestMetrics(metrics){if(!metrics||typeof metrics!=='object'||Array.isArray(metrics))return false;for(const key of ['cumulative_return','maximum_drawdown','utilization','fees','spread_cost','slippage'])if(finite(metrics[key])==null)return false;for(const key of ['annualized_return','calmar','sharpe','win_rate','average_profit','average_loss','payoff_ratio','average_holding_days'])if(metrics[key]!=null&&finite(metrics[key])==null)return false;return Number.isInteger(metrics.longest_losing_streak)&&metrics.longest_losing_streak>=0&&metrics.rejection_counts&&typeof metrics.rejection_counts==='object'&&!Array.isArray(metrics.rejection_counts)&&Object.values(metrics.rejection_counts).every(value=>Number.isInteger(value)&&value>=0)}
function validBacktestFoldSummary(summary){if(!summary||!['OK','INSUFFICIENT_SAMPLE','DATA_UNAVAILABLE'].includes(summary.status))return false;if(summary.status!=='OK')return summary.outperformance==null;return finite(summary.cumulative_return)!=null&&finite(summary.maximum_drawdown)!=null&&Number.isInteger(summary.completed_round_trips)&&summary.completed_round_trips>=0&&(summary.outperformance==null||finite(summary.outperformance)!=null)}
function validateBacktestPayload(payload,scope,symbol){
  if(!payload||payload.schema_version!==1||payload.read_only!==true||payload.scope!==scope||!['portfolio','symbol'].includes(scope))throw new Error('回测结果格式无效');
  if(scope==='symbol'&&(!validSymbol(symbol)||payload.symbol!==symbol))throw new Error('回测结果与请求标的不一致');
  if(!['OK','INSUFFICIENT_SAMPLE','DATA_UNAVAILABLE'].includes(payload.status))throw new Error('回测状态无效');
  if(payload.status!=='OK'){if(payload.outperformance!=null)throw new Error('样本不足时不得声称跑赢基准');return payload}
  const start=payload.common_start_date||payload.start_date,end=payload.common_end_date||payload.end_date,baseline=payload.baseline||payload.benchmark;
  if(!validDate(start)||!validDate(end)||finite(payload.initial_cash)==null||finite(payload.cash)==null||finite(payload.ending_equity)==null||!Number.isInteger(payload.completed_round_trips)||payload.completed_round_trips<0||!Number.isInteger(payload.uncompleted_leg_count)||payload.uncompleted_leg_count<0||!Array.isArray(payload.round_trips)||payload.round_trips.length!==payload.completed_round_trips||payload.round_trips.some(item=>finite(item?.net_pnl)==null)||!validBacktestMetrics(payload.metrics)||!baseline||finite(baseline.ending_equity)==null||finite(baseline.cumulative_return)==null||finite(payload.outperformance)==null)throw new Error('完整回测指标缺失');
  if(scope==='portfolio'){
    const walk=payload.walk_forward;if(!walk||!['OK','INSUFFICIENT_SAMPLE','DATA_UNAVAILABLE'].includes(walk.status)||walk.selected_variant!==null||!Array.isArray(walk.variants))throw new Error('样本外报告格式无效');
    if(walk.status==='OK'){const keys=new Set(walk.variants.map(variant=>JSON.stringify(variant?.parameters)));if(walk.variants.length!==81||keys.size!==81||walk.variants.some(variant=>{const p=variant?.parameters,s=variant?.stability,folds=variant?.folds;if(!p||!s||!Array.isArray(folds)||![18,20,22].includes(p.short_ma_days)||![55,60,65].includes(p.long_ma_days)||![1.75,2,2.25].includes(p.initial_stop_atr)||![2.75,3,3.25].includes(p.trailing_stop_atr)||!Number.isInteger(s.fold_count)||s.fold_count!==folds.length||!Number.isInteger(s.test_ok_count)||!Number.isInteger(s.positive_test_fold_count)||s.mean_test_return!=null&&finite(s.mean_test_return)==null)return true;return folds.some(fold=>!Number.isInteger(fold?.fold_index)||!validDate(fold.train_start_date)||!validDate(fold.train_end_date)||!validDate(fold.test_start_date)||!validDate(fold.test_end_date)||!Number.isInteger(fold.train_bar_count)||!Number.isInteger(fold.test_bar_count)||!validBacktestFoldSummary(fold.train)||!validBacktestFoldSummary(fold.test))}))throw new Error('必须完整报告81组样本外参数')}
  }
  return payload
}
function backtestRejections(metrics){const pairs=Object.entries(metrics?.rejection_counts||{}).sort((a,b)=>a[0].localeCompare(b[0]));return pairs.length?pairs.map(([name,count])=>`${escapeHtml(name)} ${backtestNumber(count,0)}`).join(' · '):'无'}
function backtestWalkMarkup(walk){if(!walk)return '';if(walk.status!=='OK')return `<div class="backtest-detail"><h4>样本外检验 · ${escapeHtml(walk.status||'—')}</h4><div class="muted">${escapeHtml(backtestReasonCopy(walk.reason||'样本外区间不足'))}</div></div>`;return `<div class="backtest-detail"><h4>滚动样本外检验 · 全部 ${walk.variants.length} 组参数（不择优）</h4><div class="muted">训练 ${backtestNumber(walk.train_days,0)} 日 · 测试 ${backtestNumber(walk.test_days,0)} 日 · 步长 ${backtestNumber(walk.step_days,0)} 日</div><div class="backtest-variants">${walk.variants.map((variant,index)=>{const p=variant.parameters,s=variant.stability;return `<details><summary>#${String(index+1).padStart(2,'0')} · MA ${backtestNumber(p.short_ma_days,0)}/${backtestNumber(p.long_ma_days,0)} · 初始/移动止损 ATR ${backtestNumber(p.initial_stop_atr,2)}/${backtestNumber(p.trailing_stop_atr,2)} · 测试均值 ${backtestPercent(s.mean_test_return)}</summary><div class="muted">有效测试 ${backtestNumber(s.test_ok_count,0)}/${backtestNumber(s.fold_count,0)} · 正收益折 ${backtestNumber(s.positive_test_fold_count,0)}</div><div class="backtest-folds">${variant.folds.map(fold=>`<div><span>第 ${backtestNumber(fold.fold_index,0)} 折</span><span>训练 ${escapeHtml(fold.train_start_date)} 至 ${escapeHtml(fold.train_end_date)}（${backtestNumber(fold.train_bar_count,0)}）</span><span>测试 ${escapeHtml(fold.test_start_date)} 至 ${escapeHtml(fold.test_end_date)}（${backtestNumber(fold.test_bar_count,0)}）</span><span>测试收益 ${backtestPercent(fold.test.cumulative_return)} · 回撤 ${backtestPercent(fold.test.maximum_drawdown)} · 往返 ${backtestNumber(fold.test.completed_round_trips,0)} · 相对基准 ${backtestPercent(fold.test.outperformance)}</span></div>`).join('')}</div></details>`}).join('')}</div></div>`}
function backtestMarkup(payload,label){
  validateBacktestPayload(payload,payload?.scope,payload?.symbol);
  const statusLabels={OK:'已完成',INSUFFICIENT_SAMPLE:'样本不足',DATA_UNAVAILABLE:'数据不可用'},status=statusLabels[payload.status]||String(payload.status||'未知');
  if(payload.status!=='OK')return `<div class="backtest-detail"><h4>${escapeHtml(label)} · ${escapeHtml(status)}</h4><div class="muted">${escapeHtml(backtestReasonCopy(payload.reason))}</div></div>`;
  const metrics=payload.metrics,baseline=payload.baseline||payload.benchmark,start=payload.common_start_date||payload.start_date,end=payload.common_end_date||payload.end_date,days=Math.max(1,(Date.parse(end)-Date.parse(start))/86400000),opportunities=payload.completed_round_trips/(days/365.2425),roundTrips=Array.isArray(payload.round_trips)?payload.round_trips:[],averageTrip=roundTrips.length?roundTrips.reduce((sum,item)=>sum+(finite(item?.net_pnl)||0),0)/roundTrips.length:null;
  const cells=[['区间',`${escapeHtml(start)} 至 ${escapeHtml(end)}`],['期末权益',backtestNumber(payload.ending_equity)],['累计 / 年化收益',`${backtestPercent(metrics.cumulative_return)} / ${backtestPercent(metrics.annualized_return)}`],['最大回撤',backtestPercent(metrics.maximum_drawdown)],['Calmar / Sharpe',`${backtestNumber(metrics.calmar)} / ${backtestNumber(metrics.sharpe)}`],['胜率',backtestPercent(metrics.win_rate)],['平均盈利 / 亏损',`${backtestNumber(metrics.average_profit)} / ${backtestNumber(metrics.average_loss)}`],['盈亏比',backtestNumber(metrics.payoff_ratio)],['平均持仓日',backtestNumber(metrics.average_holding_days)],['资金使用率',backtestPercent(metrics.utilization)],['最长连续亏损',backtestNumber(metrics.longest_losing_streak,0)],['相对基准',backtestPercent(payload.outperformance)],['完整往返 / 未完成腿',`${backtestNumber(payload.completed_round_trips,0)} / ${backtestNumber(payload.uncompleted_leg_count,0)}`],['年化机会次数',backtestNumber(opportunities)],['平均波段损益',backtestNumber(averageTrip)],['费用',backtestNumber(metrics.fees)],['滑点 / 点差',`${backtestNumber(metrics.slippage)} / ${backtestNumber(metrics.spread_cost)}`],['阻断次数',backtestRejections(metrics)]];
  return `<div class="backtest-detail"><h4>${escapeHtml(label)} · ${escapeHtml(status)}</h4><div class="backtest-summary">${cells.map(([name,value])=>`<div><span>${name}</span><strong>${value}</strong></div>`).join('')}</div><div class="muted" style="margin-top:10px">同资金不操作基准：期末 ${backtestNumber(baseline.ending_equity)} · 收益 ${backtestPercent(baseline.cumulative_return)}</div></div>${backtestWalkMarkup(payload.walk_forward)}`
}
function statusText(value){
  if(value==='SNAPSHOT_ONLY')return '持仓快照 · 仅记录';
  if(value==='SNAPSHOT_UNKNOWN')return '快照持仓风险未知';
  if(value==='PAUSED_HOLDINGS_SNAPSHOT'||value==='holdings_snapshot_only')return '快照待迁移，仓位策略停用';
const labels={OK:'正常',DATA_ERROR:'数据校验异常',REALTIME:'实时',CLOSED:'已收盘',STALE:'行情已过期',denied:'已拒绝通知',granted:'已授权通知',default:'尚未授权通知',LUNCH_BREAK:'午休',DELAYED:'行情延迟',OUTAGE:'行情断流',UNAVAILABLE:'暂无数据',UNINITIALIZED:'账户未初始化',UNKNOWN:'未知',NOT_RUN:'未执行',BLOCKED:'已阻断',TREND_BLOCKED:'趋势条件未满足',DATA_UNAVAILABLE:'数据不可用',HOLDING:'持仓观察',COOLDOWN:'止损冷静期',trend_gate:'趋势条件未满足，暂不建仓',PAUSED_MARKET_NOT_REALTIME:'当前行情非实时，盘中提醒暂停',PAUSED_PORTFOLIO_BLOCKED:'账户未就绪，暂停仓位提醒',PAUSED_DAILY_DATA:'日线未就绪',WAITING_NEXT_TRADING_DAY:'等待下一交易日',READY_TO_EXECUTE:'候选已就绪，仍需手工确认',OBSERVE_ONLY:'仅观察',ledger_unhealthy:'账户未初始化或账本未就绪',insufficient_daily_bars:'已完成日线数量不足',MINUTE_CROSSCHECK_UNAVAILABLE:'分钟交叉核验不可用'};
  return value==null||value===''?'—':labels[value]||String(value);
}
function backtestReasonCopy(reason){
  const labels={CORPORATE_ACTION_UNSUPPORTED:'原始价与复权价的比例发生变化，分红及复权处理尚未完成，暂停收益回测',INVALID_HISTORY:'历史数据未通过回测校验（含未支持的复权变化）',NO_COMPLETE_WALK_FORWARD_FOLD:'共同历史不足以形成一个完整滚动检验窗口',INSUFFICIENT_COMPLETED_DAILY_BARS:'已完成日线不足，尚不能回测',INSUFFICIENT_COMMON_HISTORY:'组合共同历史不足',NON_CONTIGUOUS_COMMON_HISTORY:'组合共同日期未对齐',NO_COMPLETED_ROUND_TRIP:'没有完成的交易回合，不能评价策略有效性',BENCHMARK_UNAVAILABLE:'同期基准不可用'};
  return labels[reason]||String(reason||'暂无可用结果');
}
function qualityMarkup(quality,coverage,evidence={}){
  if(!quality)return '<section class="quality-summary"><h3>数据质量与回测准备</h3><p class="help">暂无质量记录，不能视为已验收。</p></section>';
  const amountLabels={ESTIMATED:'估算值（非真实成交额）',PROVIDER_REPORTED:'供应商报告值（未独立核验）',UNKNOWN:'来源口径未知',MIXED:'混合口径（含不同来源）'};
  const adjustmentLabels={RATIO_CHANGED_REQUIRES_REVIEW:'复权比例有变化，需核验',RATIO_STABLE_UNVERIFIED:'样本内比例稳定，尚未核验复权方式',NO_DATA:'暂无复权数据'};
  const requested=finite(evidence.effective_risk_per_trade),risk=requested==null?'—':`${(requested*100).toFixed(2)}%`;
  const riskSource=evidence.risk_setting_source==='ACCOUNT'?'账户设置':evidence.risk_setting_source==='STRATEGY_DEFAULT'?'策略默认（账户未就绪）':'来源未知';
  const cell=(label,value)=>`<div><span>${escapeHtml(label)}</span>${escapeHtml(value)}</div>`;
  const sources=Array.isArray(quality.sources)?quality.sources:[];
  const warningLabels={INDEPENDENT_CROSSCHECK_NOT_RECORDED:'尚未记录独立数据核验',ADJUSTMENT_BASIS_UNVERIFIED:'复权方式尚未验收',AMOUNT_ESTIMATED:'成交额为估算值',UNKNOWN_SOURCE_CONTRACT:'存在口径未知的数据来源',MIXED_SOURCES:'存在混合来源，需核验口径一致性',DUPLICATE_TRADING_DATE:'存在重复交易日，不能按正常样本评价',ADJUSTMENT_RATIO_CHANGED:'复权比例发生变化，需核验权益事件',NO_DAILY_HISTORY:'暂无已完成日线',MISSING_SYMBOL_HISTORY:'组合缺少标的历史',NON_ALIGNED_COMMON_HISTORY:'组合共同日期未对齐',INSUFFICIENT_COMMON_WALK_FORWARD_SAMPLE:'组合共同样本不足以形成完整滚动窗口'};
  const warnings=[...new Set([...(Array.isArray(quality.warnings)?quality.warnings:[]),...(Array.isArray(coverage?.warnings)?coverage.warnings:[])])];
  const warningMarkup=warnings.length?`<div class="help">数据提示<ul>${warnings.map(code=>`<li>${escapeHtml(warningLabels[code]||code)}</li>`).join('')}</ul></div>`:'';
  const common=coverage?`${backtestNumber(coverage.common_bar_count,0)} / ${backtestNumber(coverage.walk_forward_required_bars,0)} 日；${backtestNumber(coverage.walk_forward_fold_count,0)} 个完整时间窗${coverage.aligned===false?'（日期未对齐或缺标的）':''}`:'暂无共同历史记录';
  return `<section class="quality-summary"><h3>数据质量与回测准备</h3><div class="evidence">${[
    cell('历史交易日',`${backtestNumber(quality.bar_count,0)} 日；指标至少 ${backtestNumber(quality.minimum_daily_bars,0)} 日`),
    cell('单标的回测样本',`单标的收益回测至少 ${backtestNumber(quality.minimum_backtest_bars,0)} 日；仅为数量门槛`),
    cell('成交额质量',amountLabels[quality.amount_quality]||'未识别口径'),
    cell('独立核验',quality.crosscheck_status==='NOT_RECORDED'?'未记录独立核验':'未识别核验状态'),
    cell('价格口径',adjustmentLabels[quality.adjustment_status]||'尚未核验'),
    cell('单标的滚动检验样本',`${backtestNumber(quality.walk_forward_fold_count,0)} 个完整时间窗；首窗至少 ${backtestNumber(quality.walk_forward_required_bars,0)} 日`),
    cell('组合共同样本',common),
    cell('有效单笔风险比例',`${risk} · ${riskSource}`),
    cell('历史范围',`${quality.start_date||'—'} 至 ${quality.end_date||'—'}`),
    cell('最近抓取时间（非核验时间）',quality.last_observed_at||'—')
  ].join('')}</div><p class="help">样本数量足够不代表策略有效，也不代表复权或成交量已经验收。当前来源分类依据历史记录中的来源标记；估算成交额不能用作独立量价核验。</p>${warningMarkup}<details><summary>查看数据来源</summary><ul>${sources.map(source=>`<li>${escapeHtml(source)}</li>`).join('')||'<li>未记录来源</li>'}</ul></details></section>`;
}
function backtestContractMarkup(payload){
  const risk=finite(payload?.strategy_parameters?.risk_per_trade);
  return `<p class="help">回测口径：次日开盘执行，不模拟盘中回到买入区的成交。回测单笔风险比例 ${risk==null?'—':(risk*100).toFixed(2)+'%'}，使用回测参数，不读取真实账户设置。盘中接近区间仅作观察，尚未按该入场方式验证收益。</p>`;
}
function indicatorMarkup(indicators){
  const value=(raw,digits=3)=>{const number=finite(raw);return number==null?'—':number.toFixed(digits)};
  if(!indicators)return '<section class="indicator-summary"><header><h3>技术指标（观察层）</h3><span class="indicator-status">暂无数据</span></header><p class="indicator-note">服务尚未返回指标快照，不代表价格为零。</p></section>';
  const status=String(indicators.status||'UNKNOWN'),statusLabel={READY:'已就绪',WARMUP:'预热中',DATA_UNAVAILABLE:'暂无完成日线',DATA_ERROR:'数据异常'}[status]||'未知';
  const statusClass=status==='READY'?'ready':status==='WARMUP'?'warmup':status==='DATA_ERROR'?'error':'';
  const cell=(label,raw,digits=3,suffix='')=>`<div class="indicator-cell"><span>${label}</span><strong>${value(raw,digits)}${finite(raw)==null?'':suffix}</strong></div>`;
  const macd=indicators.macd||{},kdj=indicators.kdj||{},rsi=indicators.rsi||{},ma=indicators.moving_averages||{},bias=indicators.bias20||{},boll=indicators.bollinger||{},volume=indicators.volume||{},weekly=indicators.weekly||{};
  const reason={INSUFFICIENT_COMPLETED_BARS:'完成日线不足120根，指标仅供预览',NO_COMPLETED_BARS:'暂无已完成日线',INVALID_COMPLETED_BARS:'日线输入未通过指标校验'}[indicators.reason]||'';
  const weeklyText=`周线 ${escapeHtml(String(weekly.status||'—'))} · ${escapeHtml(String(weekly.bar_count??0))} 根 · MA10 ${value(weekly.ma10)} / MA20 ${value(weekly.ma20)}`;
  return `<section class="indicator-summary"><header><h3>技术指标（观察层）</h3><span class="indicator-status ${statusClass}">指标状态：${escapeHtml(status)} · ${escapeHtml(statusLabel)}</span></header><p class="indicator-note">指标日期 ${escapeHtml(String(indicators.as_of_trading_date||'—'))} · 完成日线 ${escapeHtml(String(indicators.bar_count??0))} 根 · ${escapeHtml(reason||'不改变正式状态，不构成自动交易信号')}</p><div class="indicator-grid">${cell('MACD DIF',macd.dif)}${cell('MACD DEA',macd.dea)}${cell('MACD 柱体',macd.histogram)}${cell('KDJ K',kdj.k,2)}${cell('KDJ D',kdj.d,2)}${cell('KDJ J',kdj.j,2)}${cell('RSI14',rsi.rsi14,2)}${cell('MA5',ma.ma5)}${cell('MA10',ma.ma10)}${cell('MA20',ma.ma20)}${cell('MA60',ma.ma60)}${cell('BIAS20',bias.value,2,'%')}${cell('布林中轨',boll.middle)}${cell('布林上轨',boll.upper)}${cell('布林下轨',boll.lower)}${cell('20日量比',volume.ratio20,2)}${cell('20日均量',volume.ma20,0)}</div><p class="indicator-note">${weeklyText} · 周线仅作方向过滤，不单独触发信号；量比/布林用于确认回调是否健康。</p></section>`;
}
function valuationMarkup(valuation){
  const value=(raw,digits=2)=>{const number=finite(raw);return number==null?'—':number.toFixed(digits)};
  const percent=(raw)=>{const number=finite(raw);return number==null?'—':number.toFixed(2)+'%'};
  if(!valuation)return '<section class="valuation-summary"><header><h3>指数估值（观察层）</h3><span class="valuation-status">暂无数据</span></header><p class="valuation-note">服务尚未返回估值映射，不代表估值为零。</p></section>';
  const status=String(valuation.status||'UNKNOWN'),level=String(valuation.level||'UNKNOWN');
  const levelLabel={LOW:'低估',NORMAL:'中性',HIGH:'偏高',UNKNOWN:'未知'}[level]||'未知';
  const statusLabel={OK:'已读取',MISSING_VALUATION:'暂无估值',STALE:'数据偏旧',INSUFFICIENT_HISTORY:'历史不足',UNKNOWN:'未知'}[status]||status;
  const levelClass=level==='LOW'?'low':level==='HIGH'?'high':level==='NORMAL'?'normal':'';
  const impact={LOW:'估值偏低：仅作为仓位参考，不放宽趋势和止损条件。',NORMAL:'估值中性：按现有趋势、回调和成本覆盖条件执行。',HIGH:'估值偏高：不追涨，降低新增仓位，优先等待回撤。',UNKNOWN:'估值分位不可用：不改变正式策略，仅展示当前估值数据。'}[level]||'估值数据仅作观察，不构成自动交易信号。';
  const cell=(label,raw,digits=2,suffix='')=>`<div class="valuation-cell"><span>${label}</span><strong>${value(raw,digits)}${finite(raw)==null?'':suffix}</strong></div>`;
  const index=escapeHtml(String(valuation.index_name||'未映射')),code=escapeHtml(String(valuation.index_code||'—'));
  const pe5=value(valuation.pe_percentile_5y),pb5=value(valuation.pb_percentile_5y),pe10=value(valuation.pe_percentile_10y),pb10=value(valuation.pb_percentile_10y);
  return `<section class="valuation-summary"><header><h3>指数估值（观察层）</h3><span class="valuation-status ${levelClass}">估值状态：${escapeHtml(levelLabel)} · ${escapeHtml(statusLabel)}（${escapeHtml(status)}）</span></header><p class="valuation-note">关联指数 ${index} · ${code} · 数据日期 ${escapeHtml(String(valuation.as_of||'—'))}</p><div class="valuation-grid">${cell('PE-TTM',valuation.pe_ttm)}${cell('PB',valuation.pb)}${cell('股息率',valuation.dividend_yield,2,'%')}${cell('ROE-TTM',valuation.roe_ttm,2,'%')}<div class="valuation-cell"><span>5年估值分位（PE / PB）</span><strong>${pe5} / ${pb5}</strong></div><div class="valuation-cell"><span>10年估值分位（PE / PB）</span><strong>${pe10} / ${pb10}</strong></div></div><p class="valuation-impact"><strong>估值对本次信号的影响：</strong>${escapeHtml(impact)}</p><p class="valuation-note">来源：${escapeHtml(String(valuation.source||'未记录'))} · 估值为慢变量，不按分钟更新。</p></section>`;
}
function shadowMarkup(shadow){
  const stateLabels={TECHNICAL_CANDIDATE:'技术候选',OBSERVE:'观察',RANGE_BLOCKED:'震荡阻断',UNCERTAIN:'状态不确定',DATA_UNAVAILABLE:'数据不可用'};
  const reasonLabels={INSUFFICIENT_COMPLETED_BARS:'已完成日线不足',NO_COMPLETED_BARS:'暂无完成日线',DATA_QUALITY_UNKNOWN:'数据质量未知',DATA_QUALITY_UNVERIFIED:'数据质量未验收',DATA_UNHEALTHY:'日线数据不健康',ACCOUNT_UNKNOWN:'账户状态未知',RANGE_MODE:'震荡模式',UNCERTAIN_MODE:'状态不确定',TREND_NOT_CONFIRMED:'趋势未确认',TREND_SCORE_BELOW_THRESHOLD:'趋势评分不足',PULLBACK_NOT_CONFIRMED:'回调未确认',MOMENTUM_NOT_CONFIRMED:'动量未确认',MOMENTUM_SCORE_BELOW_THRESHOLD:'动量评分不足',CONFIRMATION_NOT_CONFIRMED:'确认条件未满足',VOLUME_OR_BAND_CONFIRMATION_MISSING:'量能/布林确认不足',OPPORTUNITY_NOT_CONFIRMED:'五日机会事件未确认',SHADOW_CONFIG_UNAVAILABLE:'影子配置不可用',DATA_ERROR:'数据异常'};
  const reasonText=reason=>reasonLabels[reason]||String(reason||'未知阻断');
  if(!shadow)return '<section class="shadow-summary"><header><h3>影子策略（研究层）</h3><span class="shadow-status blocked">暂无影子结果</span></header><p class="shadow-note">正式策略不受影响；影子策略未返回，不能形成候选提示。</p></section>';
  const variants=shadow.variants&&typeof shadow.variants==='object'?shadow.variants:{};
  const entries=Object.entries(variants);
  const variantHtml=entries.length?entries.map(([name,value])=>{
    const state=String(value?.state||'DATA_UNAVAILABLE'),reasons=Array.isArray(value?.blocked_reasons)?value.blocked_reasons:[];
    const evidence=value?.evidence&&typeof value.evidence==='object'?value.evidence:{};
    const score=finite(evidence.score),trendScore=finite(evidence.trend_score),momentumScore=finite(evidence.momentum_score),trigger=evidence.primary_momentum_trigger,confirmation=evidence.confirmation;
    const label=name==='HYBRID'?'混合影子':name;
    const scoreText=score==null?(trendScore==null?'—':`趋势评分 ${trendScore.toFixed(0)} · 动量评分 ${momentumScore==null?'—':momentumScore.toFixed(0)}`):score.toFixed(0);
    return `<div class="shadow-variant"><strong>${escapeHtml(label)} · ${escapeHtml(stateLabels[state]||state)}</strong><span>${value?.executable===true?'可执行（异常）':'不可执行'} · ${reasons.length?'有阻断条件':'暂无阻断条件'}</span><span>评分 ${scoreText} · 主触发 ${escapeHtml(String(trigger||'—'))} · 确认 ${escapeHtml(String(confirmation||'—'))}</span>${reasons.length?`<div class="shadow-reasons">${reasons.map(reason=>escapeHtml(reasonText(reason))).join('；')}</div>`:''}</div>`;
  }).join(''):'<div class="shadow-variant"><strong>暂无变体结果</strong><span>影子策略配置或数据不可用</span></div>';
  const reasons=Array.isArray(shadow.blocked_reasons)?shadow.blocked_reasons:[];
  const status=shadow.status==='AVAILABLE'?'已计算 · 仅研究':shadow.status==='UNAVAILABLE'?'不可用 · 仅研究':'观察中 · 仅研究';
  const opportunity=shadow.opportunity&&typeof shadow.opportunity==='object'?shadow.opportunity:null;
  const opportunityMarkup=opportunity?`<p class="shadow-note">机会事件：${escapeHtml(String(opportunity.opportunity_id||'—'))} · ${escapeHtml(String(opportunity.status||'—'))} · 到期 ${escapeHtml(String(opportunity.expiry_date||'—'))}</p>`:'<p class="shadow-note">机会事件：暂无五日回调/恢复事件，不能形成技术候选。</p>';
  return `<section class="shadow-summary"><header><h3>影子策略（研究层）</h3><span class="shadow-status ${shadow.executable===true?'blocked':''}">${escapeHtml(status)}</span></header><p class="shadow-note">V2-A/B/C 只用于对照正式策略；混合影子用于验证多因子规则；仅技术观察，不生成可执行买入（不会生成可执行买入订单）；“技术候选”不等于买入信号。</p>${opportunityMarkup}<div class="shadow-variants">${variantHtml}</div>${reasons.length?`<p class="shadow-reasons">统一阻断：${reasons.map(reason=>escapeHtml(reasonText(reason))).join('；')}</p>`:''}</section>`;
}
/* SWING_PAGE_HELPERS_END */

const VERIFIED_SYMBOLS=[['510300','沪深300ETF'],['510500','中证500ETF'],['563360','中证A500ETF'],['512100','中证1000ETF'],['159915','创业板ETF'],['588000','科创50ETF']];
const STATE_LABELS={EXIT_CANDIDATE:'退出候选',REDUCE_CANDIDATE:'减仓候选',ADD_CANDIDATE:'加仓候选',TRIAL_ENTRY_CANDIDATE:'试仓候选',TRIAL_ENTRY_OBSERVE:'试仓观察',PULLBACK_WATCH:'回调观察',UPTREND_WATCH:'趋势观察',NOT_ELIGIBLE:'暂不符合',BLOCKED:'已阻断',INSUFFICIENT_HISTORY:'数据不足',TREND_BLOCKED:'趋势条件未满足',DATA_UNAVAILABLE:'数据不可用',HOLDING:'持仓观察',COOLDOWN:'止损冷静期'};
const OVERLAY_LABELS={APPROACHING_ENTRY_ZONE:'接近计划买入区',PREDEFINED_STOP_TOUCHED:'预先确定止损位已触及'};
const GET_TIMEOUT_MS=8000,POST_TIMEOUT_MS=15000,AUXILIARY_ATTEMPTS=3,ALERT_DOM_LIMIT=80,NOTIFIED_ID_LIMIT=256;
const state=createPageState(),notifiedAlertIds=new Set(),knownActiveAlertIds=new Set(),intentRegistry=new Map();
let notificationOptIn=false,snapshotRequest=0,dailyRequest=0,backtestRequest=0,backtestLoadedKey=null,watchlistPayload={items:[]},portfolioPayload=null,alertsPayload={items:[]},eventSource=null,actionStatusTimer=null;
const watchNode=document.querySelector('#swing-watchlist'),detailNode=document.querySelector('#swing-detail'),v11SummaryNode=document.querySelector('#v11-action-summary'),errorsNode=document.querySelector('#swing-errors'),liveNode=document.querySelector('#swing-live-status'),warningNode=document.querySelector('#intraday-warning'),healthNode=document.querySelector('#health-grid'),alertNode=document.querySelector('#alert-list'),ledgerNode=document.querySelector('#ledger-list'),permissionButton=document.querySelector('#notification-permission'),backtestScopeNode=document.querySelector('#backtest-scope'),backtestRunNode=document.querySelector('#backtest-run'),backtestStatusNode=document.querySelector('#backtest-status'),backtestResultNode=document.querySelector('#backtest-result');
const fmt=(value,digits=2)=>finite(value)==null?'—':Number(value).toLocaleString('zh-CN',{minimumFractionDigits:digits,maximumFractionDigits:digits});
const pct=value=>finite(value)==null?'—':(Number(value)*100).toFixed(2)+'%';
const text=value=>statusText(value);
function updateErrors(){errorsNode.textContent=[...state.failureMessages.values()].join('\n');if(errorsNode.textContent){liveNode.className='';liveNode.textContent=errorsNode.textContent}else if(!liveNode.classList.contains('success'))liveNode.textContent='数据已同步'}
function setError(message=''){if(message)state.failureMessages.set('action',message);else state.failureMessages.delete('action');updateErrors()}
function setActionStatus(message,ok){if(actionStatusTimer){clearTimeout(actionStatusTimer);actionStatusTimer=null}if(!ok){setError(message);return}state.failureMessages.delete('action');updateErrors();liveNode.textContent=message;liveNode.className='form-result success';actionStatusTimer=setTimeout(()=>{liveNode.className='';liveNode.textContent=state.failureMessages.size?[...state.failureMessages.values()].join('\n'):'数据已同步';actionStatusTimer=null},3000)}
function markFailure(source,message){state.failureSources.add(source);state.failureMessages.set(source,message);revokeIntraday(state,message);refreshExecutionPaused(state);updateErrors();renderAll()}
function markRecovered(source){state.failureSources.delete(source);state.failureMessages.delete(source);refreshExecutionPaused(state);updateErrors()}
function candidatePriority(item){const stateName=item.formal_state;return stateName==='EXIT_CANDIDATE'?0:item.intraday_overlay==='PREDEFINED_STOP_TOUCHED'?0:['REDUCE_CANDIDATE','ADD_CANDIDATE','TRIAL_ENTRY_CANDIDATE','TRIAL_ENTRY_OBSERVE'].includes(stateName)?1:stateName==='PULLBACK_WATCH'?2:stateName==='UPTREND_WATCH'?3:4}
function itemFor(symbol){return (state.snapshot?.items||[]).find(item=>item.symbol===symbol)||null}
function verifiedSymbols(){const available=state.snapshot?.available_symbols;return Array.isArray(available)?available.filter(item=>validSymbol(item.symbol)).map(item=>[item.symbol,item.name||item.symbol]):[...VERIFIED_SYMBOLS]}
function renderPendingWatchlist(){for(const item of state.snapshot?.pending_instruments||[]){if(!validSymbol(item.symbol))continue;const row=document.createElement('div');row.className='watch-row';const label=document.createElement('span');label.className='watch-select';label.textContent=`${item.name||item.symbol} ${item.symbol} · 待接入行情`;const toggle=document.createElement('input');toggle.type='checkbox';toggle.disabled=true;toggle.setAttribute('aria-label',`${item.name||item.symbol}待接入行情，不可启用`);row.append(label,toggle);watchNode.append(row)}}
function renderPortfolioWriteLock(){
  const blocked=holdingsSnapshotPresent(state.snapshot);
  for(const id of ['portfolio-initialize-form','trade-form','reverse-form']){
    const form=document.getElementById(id);form.hidden=blocked;
    if(blocked){for(const control of form.querySelectorAll('input,select,button')){if(!control.disabled)control.dataset.snapshotDisabled='true';control.disabled=true}}
    else for(const control of form.querySelectorAll('[data-snapshot-disabled]')){control.disabled=false;delete control.dataset.snapshotDisabled}
  }
}
function renderWatchlist(){
  watchNode.replaceChildren();const enabled=new Map((watchlistPayload.items||[]).filter(item=>validSymbol(item.symbol)).map(item=>[item.symbol,item.enabled===true]));
  const ordered=verifiedSymbols().sort((a,b)=>candidatePriority(itemFor(a[0])||{})-candidatePriority(itemFor(b[0])||{}));
  for(const [symbol,name] of ordered){const row=document.createElement('div');row.className='watch-row'+(symbol===state.selectedSymbol?' selected':'');const select=document.createElement('button');select.type='button';select.className='watch-select';select.dataset.symbol=symbol;const info=itemFor(symbol),stateName=info?.formal_state||'NOT_ELIGIBLE',overlay=info?.intraday_overlay?.overlay||info?.intraday_overlay;const dot=document.createElement('i');dot.className='state-dot '+(overlay==='PREDEFINED_STOP_TOUCHED'||stateName==='EXIT_CANDIDATE'?'red':['REDUCE_CANDIDATE','ADD_CANDIDATE','TRIAL_ENTRY_CANDIDATE','TRIAL_ENTRY_OBSERVE','PULLBACK_WATCH'].includes(stateName)?'yellow':stateName==='UPTREND_WATCH'?'green':'');dot.setAttribute('aria-hidden','true');const strong=document.createElement('strong');strong.textContent=name;const small=document.createElement('span');small.textContent=`${symbol} · ${STATE_LABELS[stateName]||stateName}`;select.append(dot,strong,small);select.addEventListener('click',()=>selectSymbol(symbol));const label=document.createElement('label');label.className='toggle-label';const toggle=document.createElement('input');toggle.type='checkbox';toggle.checked=enabled.get(symbol)===true;toggle.disabled=!toggle.checked&&!watchActivationAllowed(state.snapshot,symbol);if(!toggle.checked){const preparation=historyPreparationCopy(state.snapshot,symbol);toggle.title=preparation.reason;select.title=preparation.reason;small.textContent=`${symbol} · ${preparation.label}`}toggle.setAttribute('aria-label',`${name}监控开关`);toggle.addEventListener('change',()=>toggleWatch(symbol,toggle.checked,toggle));label.append(toggle,document.createTextNode('监控'));row.append(select,label);watchNode.append(row)}
}
function chartHtml(symbol){
  const bars=[...(state.dailyBySymbol.get(symbol)?.values()||[])];
  if(!bars.length)return '<div class="empty">无日线数据：等待已完成交易日行情</div>';
  if(bars.length<60)return `<div class="empty">日线数据不足：当前 ${bars.length} 根，至少需要 60 根计算 MA60</div>`;
  const series=chartSeries(bars),points=series.points,latest=bars.sort((a,b)=>a.trading_date.localeCompare(b.trading_date))[bars.length-1],adjustment_scale=latest.close/latest.adjusted_close;
  if(!Number.isFinite(adjustment_scale)||adjustment_scale<=0)return '<div class="empty">日线复权比例无效，图表暂停</div>';
  const decision=itemFor(symbol)?.formal_decision||state.lastFormalPlans.get(symbol)?.formal_decision||{},evidence=decision.evidence||{};
  const entryLow=finite(decision.planned_entry_low??evidence.entry_zone_low_raw),entryHigh=finite(decision.planned_entry_high??evidence.entry_zone_high_raw),hardStop=finite(evidence.hard_stop_raw_mapped??evidence.initial_stop_raw),trailingStop=finite(evidence.trailing_stop_raw),w=860,h=340,left=62,right=18,top=18,bottom=38,pw=w-left-right,ph=h-top-bottom;
  const values=points.flatMap(point=>[point.close,point.ma20,point.ma60]).concat([entryLow,entryHigh,hardStop,trailingStop]).filter(Number.isFinite),lo=Math.min(...values),hi=Math.max(...values),span=hi-lo||1,x=index=>left+index*pw/Math.max(1,points.length-1),y=value=>top+(hi-value)*ph/span,path=key=>points.map((point,index)=>Number.isFinite(point[key])?`${index&&Number.isFinite(points[index-1][key])?'L':'M'}${x(index).toFixed(1)} ${y(point[key]).toFixed(1)}`:'').join(' ');
  const yTicks=Array.from({length:5},(_,index)=>{const value=hi-span*index/4,py=y(value);return `<g><line class="grid" x1="${left}" y1="${py}" x2="${w-right}" y2="${py}"/><text class="axis-label" x="${left-7}" y="${py+4}" text-anchor="end">${escapeHtml(fmt(value,3))}</text></g>`}).join('');
  const tickIndexes=[0,Math.floor((points.length-1)/2),points.length-1],xTicks=tickIndexes.map(index=>`<g><line class="axis" x1="${x(index)}" y1="${h-bottom}" x2="${x(index)}" y2="${h-bottom+5}"/><text class="axis-label" x="${x(index)}" y="${h-8}" text-anchor="middle">${escapeHtml(points[index].date.slice(5))}</text></g>`).join('');
  const horizontal=(value,cls,label)=>value==null?'':`<g><line class="${cls}" x1="${left}" y1="${y(value)}" x2="${w-right}" y2="${y(value)}"/><text class="axis-label" x="${w-right-3}" y="${y(value)-4}" text-anchor="end">${escapeHtml(label)}</text></g>`;
  const entry=entryLow==null||entryHigh==null?'':`<rect class="entry-zone" x="${left}" y="${y(entryHigh)}" width="${pw}" height="${Math.max(1,y(entryLow)-y(entryHigh))}"/>`;
  window.swingChartPoints=points;
  return `<div class="chart-title"><strong>最近120个完成交易日</strong><span class="muted">最新交易日价格基准</span></div><div class="legend"><span><i class="close"></i>复权收盘（最新交易日基准）</span><span><i class="ma20"></i>MA20</span><span><i class="ma60"></i>MA60</span><span><i class="entry"></i>计划买入区</span><span>硬止损 / 移动止损</span></div><svg id="swing-chart" class="chart-svg" role="img" aria-label="${escapeHtml(symbol)}最近完成日线复权收盘（最新交易日基准）、均线、计划买入区与止损线" tabindex="0" viewBox="0 0 ${w} ${h}">${yTicks}${entry}${horizontal(hardStop,'hard-stop','硬止损')}${horizontal(trailingStop,'trailing-stop','移动止损')}<path class="close-line" d="${path('close')}"/><path class="ma20-line" d="${path('ma20')}"/><path class="ma60-line" d="${path('ma60')}"/><line id="chart-cursor" class="chart-cursor" y1="${top}" y2="${h-bottom}"/><g>${xTicks}</g><rect id="chart-hit" x="${left}" y="${top}" width="${pw}" height="${ph}" fill="transparent" data-left="${left}" data-width="${pw}"/></svg><div id="chart-tooltip" class="chart-tooltip" role="status"></div>`;
}
function planHtml(item){const decision=item?.formal_decision||state.lastFormalPlans.get(state.selectedSymbol)?.formal_decision||{},evidence=decision.evidence||{},date=item?.signal_data_date||state.lastFormalPlans.get(state.selectedSymbol)?.as_of_trading_date;if(holdingsSnapshotPresent(state.snapshot)){const gate=(key,label)=>`<div><dt>${label}</dt><dd>${evidence[key]===true?'满足':evidence[key]===false?'未满足':'—'}</dd></div>`;return `<div class="plan"><h3>技术观察 · 不可执行</h3><p class="muted">持仓快照未迁移：买入日期、止损及风险未知，不提供建议份额或盘中操作提醒。下面只展示日线门槛。</p><dl><dt>信号数据日期</dt><dd>${escapeHtml(text(date))}</dd><dt>技术买入区</dt><dd>${fmt(decision.planned_entry_low,3)} – ${fmt(decision.planned_entry_high,3)}</dd>${gate('trend_ma20_above_ma60','趋势 MA20>MA60')}${gate('pullback_low_touched','回调触及')}${gate('reclaim_close_above_ma20','收回 MA20')}${gate('confirmation_above_previous_high','收回确认')}${gate('anti_chase_ok','反追高')}</dl></div>`}const copy=executionStatusCopy(state);return `<div class="plan"><h3>下一交易日计划</h3><p class="${state.safetyPaused?'paused-note':'muted'}">${escapeHtml(copy)}</p><dl><dt>信号数据日期</dt><dd>${escapeHtml(text(date))}</dd><dt>买入区</dt><dd>${fmt(decision.planned_entry_low,3)} – ${fmt(decision.planned_entry_high,3)}</dd><dt>建议份额</dt><dd>${fmt(decision.planned_shares,0)}</dd><dt>预计资金</dt><dd>${finite(decision.planned_shares)!=null&&finite(decision.planned_entry_high)!=null?fmt(decision.planned_shares*decision.planned_entry_high):'—'}</dd><dt>初始/保护止损</dt><dd>${fmt(decision.planned_stop??evidence.protective_stop_raw,3)}</dd><dt>本次风险</dt><dd>${pct(decision.planned_risk_rate)}</dd><dt>2R 减仓位</dt><dd>${fmt(decision.first_reduce_price,3)}</dd></dl></div>`}
function v11Label(value){return ({DATA_UNAVAILABLE:'数据不可用',OBSERVE:'观察',TECHNICAL_CANDIDATE:'技术候选',ACTION_CANDIDATE:'可执行候选（仍需人工确认）',POSITION_ACTION:'持仓动作',UNCERTAIN:'状态不确定'})[value]||text(value)}
function v11SetupLabel(value){return ({A_PULLBACK:'A 回调恢复',B_BREAKOUT:'B 箱体突破',NONE:'无有效形态'})[value]||text(value)}
function v11ActionLabel(value){return ({ENTRY:'建仓候选',ENTRY_CANDIDATE:'建仓候选',TOP_UP:'加仓候选',REDUCE:'减仓候选',EXIT:'退出候选',OBSERVE:'继续观察',HOLD:'继续持有'})[value]||text(value)}
function renderV11Summary(){
  if(!v11SummaryNode)return;
  const item=itemFor(state.selectedSymbol),v=item?.v11;
  if(!item||!v){v11SummaryNode.innerHTML='<div class="empty">尚无 V11 数据；等待已完成日线发布。</div>';return}
  const decision=v.decision||{},evidence=v.evidence||{},blocked=Array.isArray(v.blocked_reasons)?v.blocked_reasons:[],dataDate=v.as_of_trading_date||'—',asOf=v.as_of_kind||'—',status=v11Label(decision.state||v.status),setup=v11SetupLabel(decision.setup),action=v11ActionLabel(decision.action),planned=finite(decision.planned_notional_cny),stop=finite(decision.stop_price);
  const reasonHtml=blocked.length?`<details><summary>阻断原因（${blocked.length}）</summary><div class="muted">${blocked.map(reason=>`<div>${escapeHtml(reason)}</div>`).join('')}</div></details>`:'<div class="muted">当前没有阻断码，但仍需人工确认。</div>';
  const evidenceHtml=`<details><summary>查看判断依据</summary><pre>${escapeHtml(JSON.stringify({environment:evidence.environment_state||null,bar_count:evidence.bar_count||null,macd_trigger:evidence.macd_trigger??null,rsi_trigger:evidence.rsi_trigger??null,volume_ratio20:evidence.volume_ratio20??null,forced_half_size:evidence.forced_half_size??false},null,2))}</pre></details>`;
  v11SummaryNode.innerHTML=`<div><span>V11 状态</span><strong>${escapeHtml(status)}</strong></div><div><span>数据时点</span>${escapeHtml(asOf)} · ${escapeHtml(dataDate)}</div><div><span>形态 / 行动</span>${escapeHtml(setup)} · ${escapeHtml(action)}</div><div><span>计划金额 / 止损</span>${planned==null?'—':fmt(planned)} 元 · ${stop==null?'—':fmt(stop,3)}</div><div><span>执行约束</span>人工确认 · 不支持自动下单</div><div><span>可执行候选</span>${decision.executable===true?'需二次确认':'否（影子策略）'}</div><div>${reasonHtml}${evidenceHtml}</div>`;
}
function renderDetail(){
  const item=itemFor(state.selectedSymbol);if(!item){const preparation=historyPreparationCopy(state.snapshot,state.selectedSymbol);detailNode.innerHTML=`<div class="empty"><strong>${escapeHtml(preparation.label)}</strong><p>${escapeHtml(preparation.reason)}</p></div>`+selectedHoldingMarkup(state.snapshot,state.selectedSymbol);return}
  const decision=item.formal_decision||{},evidence=decision.evidence||{},blocked=(item.blocked_reasons||decision.blocked_reasons||[]).map(statusText),overlay=item.intraday_overlay?.overlay||item.intraday_overlay;
  const notice=executionNotice(state),statusClass=notice.tone==='danger'?'danger':notice.tone==='warning'?'paused':state.selectedExecutable?'ready':'observe';
  detailNode.innerHTML=`<div class="detail-head"><div><div class="title-symbol">${escapeHtml(item.symbol)}</div><h2>${escapeHtml(item.name||item.symbol)}</h2><div class="muted">信号数据日期 ${escapeHtml(text(item.signal_data_date))} · 行情时间 ${escapeHtml(text(item.current_price_time))}</div><div class="today-change ${finite(item.change_pct)!=null&&item.change_pct>=0?'up':'down'}">今日涨跌幅：${escapeHtml(pct(item.change_pct))} · ${escapeHtml(statusText(item.intraday_health_status))}</div></div><span class="status-pill ${statusClass}">${escapeHtml(notice.summary||executionStatusCopy(state))}</span></div>${overlay?`<p class="alert-banner">${escapeHtml(OVERLAY_LABELS[overlay]||overlay)}</p>`:''}<div class="chart-layout"><div class="chart-panel"><div class="muted">正式策略（SWING_V1）</div>${chartHtml(item.symbol)}</div>${planHtml(item)}</div>${selectedHoldingMarkup(state.snapshot,state.selectedSymbol)}<div class="evidence"><div><span>正式状态</span>${escapeHtml(STATE_LABELS[item.formal_state]||item.formal_state)}</div><div><span>MA20</span>${fmt(evidence.ma20_raw,3)}</div><div><span>MA60</span>${fmt(evidence.ma60_raw,3)}</div><div><span>ATR14</span>${fmt(evidence.atr14_raw,3)}</div><div><span>距 MA20</span>${fmt(evidence.close_ma20_distance_raw,3)}</div><div><span>趋势 MA20>MA60</span>${evidence.trend_ma20_above_ma60===true?'满足':evidence.trend_ma20_above_ma60===false?'未满足':'—'}</div><div><span>回调触及</span>${evidence.pullback_low_touched===true?'满足':evidence.pullback_low_touched===false?'未满足':'—'}</div><div><span>收回 MA20</span>${evidence.reclaim_close_above_ma20===true?'满足':evidence.reclaim_close_above_ma20===false?'未满足':'—'}</div><div><span>收回确认</span>${evidence.confirmation_above_previous_high===true?'满足':evidence.confirmation_above_previous_high===false?'未满足':'—'}</div><div><span>反追高</span>${evidence.anti_chase_ok===true?'满足':evidence.anti_chase_ok===false?'未满足':'—'}</div><div><span>风险预算</span>${holdingsSnapshotPresent(state.snapshot)?'未知':pct(decision.planned_risk_rate)}</div><div><span>盘中行情状态</span>${escapeHtml(statusText(item.intraday_health_status))}</div><div><span>执行状态</span>${escapeHtml(text(item.execution_status))}</div><div><span>阻断原因</span>${blocked.length?blocked.map(escapeHtml).join('；'):'—'}</div></div>`;
  detailNode.insertAdjacentHTML('beforeend',indicatorMarkup(item.indicators));
  detailNode.insertAdjacentHTML('beforeend',valuationMarkup(item.valuation));
  detailNode.insertAdjacentHTML('beforeend',shadowMarkup(item.shadow));
  detailNode.insertAdjacentHTML('beforeend',qualityMarkup(item.data_quality,state.snapshot?.history_coverage,evidence));
  bindChartTooltip();
}
function bindChartTooltip(){const svg=document.querySelector('#swing-chart'),hit=document.querySelector('#chart-hit'),tooltip=document.querySelector('#chart-tooltip'),cursor=document.querySelector('#chart-cursor'),points=window.swingChartPoints||[];if(!svg||!hit||!tooltip||!cursor||!points.length)return;let index=points.length-1;const show=(next,clientX,clientY)=>{index=Math.max(0,Math.min(points.length-1,next));const point=points[index],left=Number(hit.dataset.left),width=Number(hit.dataset.width),x=left+index*width/Math.max(1,points.length-1);cursor.setAttribute('x1',x);cursor.setAttribute('x2',x);cursor.style.display='block';tooltip.textContent=`${point.date}\n复权收盘 ${fmt(point.close,3)}\n当日原始收盘 ${fmt(point.rawClose,3)}\nMA20 ${fmt(point.ma20,3)}\nMA60 ${fmt(point.ma60,3)}`;tooltip.style.display='block';const rect=svg.getBoundingClientRect();tooltip.style.left=`${Math.max(4,(clientX??rect.left+x*rect.width/860)-rect.left+8)}px`;tooltip.style.top=`${Math.max(4,(clientY??rect.top+24)-rect.top+8)}px`};const locate=clientX=>{const plot=hit.getBoundingClientRect();if(plot.width>0)return pointerIndex(clientX,plot.left,plot.width,points.length);const rect=svg.getBoundingClientRect(),viewWidth=svg.viewBox?.baseVal?.width||860,left=rect.left+Number(hit.dataset.left)*rect.width/viewWidth,width=Number(hit.dataset.width)*rect.width/viewWidth;return pointerIndex(clientX,left,width,points.length)};svg.addEventListener('pointermove',event=>show(locate(event.clientX),event.clientX,event.clientY));svg.addEventListener('pointerleave',()=>{cursor.style.display='none';tooltip.style.display='none'});svg.addEventListener('touchmove',event=>{const touch=event.touches[0];if(touch)show(locate(touch.clientX),touch.clientX,touch.clientY)},{passive:true});svg.addEventListener('focus',()=>show(index));svg.addEventListener('keydown',event=>{if(event.key==='ArrowLeft'||event.key==='ArrowRight'){event.preventDefault();show(index+(event.key==='ArrowRight'?1:-1))}})}
function renderDiagnostics(){
  const d=state.snapshot?.diagnostics;
  if(!d)return;
  const countList=counts=>Object.entries(counts||{}).sort((a,b)=>b[1]-a[1]).map(([reason,count])=>`${escapeHtml(reason)}：${escapeHtml(String(count))}`).join('；')||'无';
  const coverage=d.coverage||{};
  healthNode.insertAdjacentHTML('beforeend',`<div><span>研究质量</span>未独立验证</div><div><span>策略有效性</span>未验证</div><div><span>共同历史 / 首窗所需</span>${escapeHtml(String(coverage.common_bar_count??'—'))} / ${escapeHtml(String(coverage.required??'—'))}</div><div style="grid-column:1/-1"><span>当前快照诊断（不是历史触发率）</span>${d.item_status==='NO_DATA'?'暂无标的数据':`质量警告：${countList(d.warning_counts)}<br>策略拦截：${countList(d.blocked_reason_counts)}`}<br><small>样本长度达标不代表完成样本外验证；服务正常不代表数据可信。</small></div>`);
}
function renderHealth(){const h=state.snapshot?.health||{};const permission=('Notification'in window)?Notification.permission:'不支持';healthNode.innerHTML=`<div><span>服务健康</span>${escapeHtml(statusText(h.service))}</div><div><span>日线完整性（非独立验收）</span>${escapeHtml(statusText(h.daily))} · ${escapeHtml(text(state.snapshot?.as_of_trading_date))}</div><div><span>行情状态</span>${escapeHtml(statusText(h.intraday))}</div><div><span>策略配置</span>${escapeHtml(statusText(h.configuration))}</div><div><span>组合账本</span>${escapeHtml(statusText(h.portfolio))}</div><div><span>浏览器通知</span>${escapeHtml(statusText(permission))}</div>`}
function renderAlerts(){const source=alertsPayload?.items||state.snapshot?.alerts||[],items=boundedAlerts(alertsForDisplay(source,state).filter(alert=>!alert.retracted),ALERT_DOM_LIMIT);if(!items.length){alertNode.innerHTML='<div class="empty">暂无提醒</div>';return}alertNode.innerHTML=items.map(alert=>`<article class="alert-item"><strong>${escapeHtml(alert.label||alert.state)}</strong><div class="muted">${escapeHtml(alert.symbol)} · ${escapeHtml(alert.trading_date)} · ${escapeHtml(alert.scope)}</div><details><summary>查看判断依据</summary><pre>${escapeHtml(JSON.stringify(alert.evidence||{},null,2))}</pre></details><div class="alert-actions"><button class="button secondary" data-alert-action="acknowledge" data-alert-id="${escapeHtml(alert.alert_id)}" data-alert-scope="${escapeHtml(alert.scope)}" data-alert-state="${escapeHtml(alert.state)}" type="button">标记已读</button><button class="button secondary" data-alert-action="ignore" data-alert-id="${escapeHtml(alert.alert_id)}" data-alert-scope="${escapeHtml(alert.scope)}" data-alert-state="${escapeHtml(alert.state)}" type="button">忽略本次</button><button class="button" data-alert-trade="${escapeHtml(alert.symbol)}" data-alert-scope="${escapeHtml(alert.scope)}" data-alert-state="${escapeHtml(alert.state)}" type="button">记录成交</button></div><div class="form-result" aria-live="polite"></div></article>`).join('');if(holdingsSnapshotPresent(state.snapshot))for(const button of alertNode.querySelectorAll('button'))button.disabled=true;for(const button of alertNode.querySelectorAll('[data-alert-action]'))button.addEventListener('click',()=>transitionAlert(button));for(const button of alertNode.querySelectorAll('[data-alert-trade]'))button.addEventListener('click',()=>openTradeForAlert(button.dataset.alertTrade,button.dataset.alertScope,button.dataset.alertState))}
function renderLedger(){if(holdingsSnapshotPresent(state.snapshot)){ledgerNode.innerHTML='<div class="empty">实际持仓已记录为独立快照，未生成历史成交；旧账本不参与策略。需完成明确迁移后才能启用记账入口。</div>';return}const projection=portfolioPayload?.projection||state.snapshot?.portfolio;if(!projection){ledgerNode.innerHTML='<div class="empty">账户尚未初始化；请先完成初始化波段账户。</div>';return}const positions=Object.values(projection.positions||{});ledgerNode.innerHTML=`<div class="ledger-item"><strong>${escapeHtml(projection.name)}</strong><div class="muted">账本日期 ${escapeHtml(text(projection.as_of_trading_date))} · 最近事件 ${escapeHtml(text(projection.last_event_id))}</div><div>现金 ${fmt(projection.cash)} · 已实现损益 ${fmt(projection.realized_pnl)}</div></div>`+(positions.length?positions.map(position=>`<div class="ledger-item"><strong>${escapeHtml(position.symbol)}</strong><div>持仓 ${fmt(position.shares,0)} · 可卖 ${fmt(position.sellable_shares,0)} · 成本 ${fmt(position.average_cost,3)} · 计划风险 ${fmt(position.planned_risk)}</div></div>`).join(''):'<div class="empty">当前没有持仓记录</div>')}
function renderWarning(){const notice=executionNotice(state);warningNode.hidden=notice.tone==='none';warningNode.className=`card execution-notice notice-${notice.tone}`;warningNode.setAttribute('role',notice.tone==='danger'?'alert':'status');warningNode.innerHTML=executionWarningMarkup(state)}
function renderAll(){renderWatchlist();renderPendingWatchlist();renderV11Summary();renderDetail();renderHealth();renderDiagnostics();renderAlerts();renderLedger();renderWarning();renderPortfolioWriteLock()}
function selectSymbol(symbol){if(!validSymbol(symbol))return;state.selectedSymbol=symbol;refreshExecutionPaused(state);const tradeSelect=document.querySelector('#trade-form [name="symbol"]');if(tradeSelect)tradeSelect.value=symbol;renderAll();loadDaily(symbol).catch(()=>{});if(backtestScopeNode.value==='symbol'&&!document.querySelector('#backtest-panel').hidden){backtestLoadedKey=null;loadBacktest()}}
async function jsonResponse(response){let value;try{value=await response.json()}catch(_error){const error=new Error('服务返回了无法解析的数据');error.status=response.status;throw error}if(!response.ok){const error=new Error(value.message||value.error||'请求失败');error.status=response.status;throw error}return value}
async function fetchWithTimeout(path,options={},timeoutMs=GET_TIMEOUT_MS){const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),timeoutMs);try{return await fetch(path,{...options,signal:controller.signal})}catch(error){if(error?.name==='AbortError')throw new Error('请求超时，请重试');throw error}finally{clearTimeout(timer)}}
async function postLocalRecord(path,payload,{idempotencyKey=null}={}){const headers=idempotencyKey?{'Content-Type':'application/json','Idempotency-Key':idempotencyKey}:{'Content-Type':'application/json'};return jsonResponse(await fetchWithTimeout(path,{method:'POST',headers,body:JSON.stringify(payload)},POST_TIMEOUT_MS))}
async function submitIntent(owner,path,payload){if(holdingsSnapshotPresent(state.snapshot)&&(path==='/api/swing/portfolio/initialize'||path.startsWith('/api/swing/trades')))throw new Error('已导入持仓快照，需迁移后才能记账');const record=prepareIntent(intentRegistry,owner,path,payload);if(record.promise)return record.promise;record.promise=postLocalRecord(path,payload,{idempotencyKey:record.key});try{const result=await record.promise;settleIntentSuccess(intentRegistry,owner,record.key);return result}catch(error){record.promise=null;throw error}}
async function toggleWatch(symbol,enabled,control){if(!validSymbol(symbol)){control.checked=!enabled;return}control.disabled=true;try{await postLocalRecord('/api/swing/watchlist',{symbol,enabled});setActionStatus('监控列表已保存',true);await loadSnapshot(false)}catch(error){control.checked=!enabled;setActionStatus(`监控列表保存失败：${error.message}`,false)}finally{control.disabled=false}}
async function loadDaily(symbol){if(!validSymbol(symbol))throw new Error('日线标的无效');const token=++dailyRequest,since=state.dailyRevisions.get(symbol)??0;try{const payload=await jsonResponse(await fetchWithTimeout(`/api/swing/daily-quotes?symbol=${encodeURIComponent(symbol)}&since=${encodeURIComponent(String(since))}&limit=179`,{cache:'no-store'},GET_TIMEOUT_MS));if(token!==dailyRequest||symbol!==state.selectedSymbol)return;applyDailyPayload(state,payload);markRecovered('daily');renderAll();invalidateBacktestIfChanged()}catch(error){if(token===dailyRequest)markFailure('daily',`日线数据读取失败：${error.message}`);throw error}}
function renderBacktestResult(payload,label){backtestResultNode.innerHTML=backtestContractMarkup(payload)+backtestMarkup(payload,label)}
function currentBacktestKey(){return backtestCacheKey(backtestScopeNode.value,state.selectedSymbol,state)}
function invalidateBacktestIfChanged(){if(backtestLoadedKey!=null&&backtestLoadedKey!==currentBacktestKey()){backtestLoadedKey=null;backtestStatusNode.className='form-result';backtestStatusNode.textContent='数据版本已更新，请重新载入';backtestResultNode.innerHTML='<div class="empty">旧回测结果已撤下</div>'}}
async function loadBacktest(force=false){const scope=backtestScopeNode.value,symbol=state.selectedSymbol,path=backtestQuery(scope,symbol),key=backtestCacheKey(scope,symbol,state);const token=++backtestRequest,label=scope==='portfolio'?'已启用ETF组合':`${verifiedSymbols().find(item=>item[0]===symbol)?.[1]||symbol} ${symbol}`;backtestRunNode.disabled=true;backtestStatusNode.className='form-result';backtestStatusNode.textContent='正在计算只读回测…';backtestResultNode.innerHTML='<div class="empty">正在载入回测结果</div>';try{const payload=await jsonResponse(await fetchWithTimeout(path,{cache:'no-store'},POST_TIMEOUT_MS));if(token!==backtestRequest)return false;validateBacktestPayload(payload,scope,symbol);if(!backtestResponseCurrent(token,backtestRequest,key,backtestCacheKey(scope,symbol,state))){backtestLoadedKey=null;backtestStatusNode.textContent='数据版本已更新，请重新运行';backtestResultNode.innerHTML='<div class="empty">旧回测响应已丢弃</div>';return false}renderBacktestResult(payload,label);backtestLoadedKey=key;backtestStatusNode.className='form-result success';backtestStatusNode.textContent='回测已载入';return true}catch(error){if(token===backtestRequest){backtestResultNode.innerHTML=`<div class="backtest-error">回测读取失败：${escapeHtml(error.message)}</div>`;backtestStatusNode.className='form-result error';backtestStatusNode.textContent='仅回测面板不可用，监控与正式提醒不受影响'}return false}finally{if(token===backtestRequest)backtestRunNode.disabled=false}}
function syncActiveAlerts(payload){const current=(payload.active_alerts||[]).filter(alert=>alert&&alert.currently_active!==false&&!alert.retracted);for(const alert of current){const isNew=!knownActiveAlertIds.has(alert.alert_id);knownActiveAlertIds.add(alert.alert_id);if(isNew&&shouldNotifyAlert(alert,notificationOptIn,('Notification'in window)?Notification.permission:'denied',notifiedAlertIds,state.safetyPaused)){notifiedAlertIds.add(alert.alert_id);new Notification(`波段提醒 · ${alert.symbol}`,{body:alert.label||alert.state})}}const activeIds=new Set(current.map(alert=>alert.alert_id));for(const id of [...knownActiveAlertIds])if(!activeIds.has(id))knownActiveAlertIds.delete(id);pruneRememberedAlertIds(notifiedAlertIds,activeIds,NOTIFIED_ID_LIMIT);pruneRememberedAlertIds(knownActiveAlertIds,activeIds,NOTIFIED_ID_LIMIT)}
async function fetchAuxiliaryOnce(){const [watch,portfolio,alerts]=await Promise.all(['/api/swing/watchlist','/api/swing/portfolio','/api/swing/alerts?limit=80'].map(path=>fetchWithTimeout(path,{cache:'no-store'},GET_TIMEOUT_MS).then(jsonResponse)));return {watch,portfolio,alerts}}
async function loadAuxiliary(expectedRevision){let lastError=null;for(let attempt=0;attempt<AUXILIARY_ATTEMPTS;attempt++){try{const bundle=await fetchAuxiliaryOnce();for(const view of [bundle.watch,bundle.portfolio,bundle.alerts])if(Number(view.revision)!==Number(expectedRevision))throw new Error(`读模型版本 ${view.revision} 尚未追上快照 ${expectedRevision}`);return bundle}catch(error){lastError=error;if(attempt+1<AUXILIARY_ATTEMPTS)await new Promise(resolve=>setTimeout(resolve,80*(2**attempt)))}}throw lastError||new Error('波段读模型同步失败')}
function syncAuxiliaryGlobals(){if(!state.auxiliary)return;watchlistPayload=state.auxiliary.watch;portfolioPayload=state.auxiliary.portfolio;alertsPayload=state.auxiliary.alerts}
function clearRecoveredSources(sources){for(const source of sources){state.failureSources.delete(source);state.failureMessages.delete(source)}}
function installReadModel(payload,bundle,reset,recoveredSources){if(!validateReadModelBundle(state,payload,bundle,reset))return false;clearRecoveredSources(recoveredSources);if(reset){state.dailyBySymbol.clear();state.dailyRevisions.clear()}if(!applyReadModelBundle(state,payload,bundle,reset))return false;syncAuxiliaryGlobals();refreshExecutionPaused(state);updateErrors();syncActiveAlerts(state.snapshot);renderAll();invalidateBacktestIfChanged();return true}
async function loadSnapshot(authoritative=true){const token=++snapshotRequest;try{const payload=await jsonResponse(await fetchWithTimeout('/api/swing/snapshot',{cache:'no-store'},GET_TIMEOUT_MS)),bundle=await loadAuxiliary(payload.revision);if(token!==snapshotRequest)return false;if(!installReadModel(payload,bundle,authoritative,['snapshot']))return false;if(state.selectedSymbol)await loadDaily(state.selectedSymbol).catch(()=>{});return true}catch(error){if(token===snapshotRequest)markFailure('snapshot',`波段快照读取失败：${error.message}`);return false}}
async function applySsePayload(payload,reset){const token=++snapshotRequest;try{const bundle=await loadAuxiliary(payload?.revision);if(token!==snapshotRequest)return false;if(!installReadModel(payload,bundle,reset,['sse','snapshot']))return false;if(state.selectedSymbol)loadDaily(state.selectedSymbol).catch(()=>{});return true}catch(error){if(token===snapshotRequest)markFailure('sse',`事件读模型同步失败：${error.message}`);return false}}
function connectEvents(){if(eventSource)eventSource.close();eventSource=new EventSource('/api/swing/events');const accept=reset=>event=>{let payload;try{payload=JSON.parse(event.data)}catch(error){markFailure('sse',`事件数据无效：${error.message}`);return}applySsePayload(payload,reset).catch(error=>markFailure('sse',`事件数据无效：${error.message}`))};eventSource.addEventListener('snapshot',accept(false));eventSource.addEventListener('delta',accept(false));eventSource.addEventListener('reset',accept(true));eventSource.onopen=()=>{eventStreamOpened(state);liveNode.textContent='事件连接已建立，等待有效快照'};eventSource.onerror=()=>markFailure('sse','波段事件连接中断，执行已暂停')}
function formPayload(form){return Object.fromEntries(new FormData(form).entries())}
function resultNode(form){return form.querySelector('.form-result')}
function showFormResult(form,message,ok){const node=resultNode(form);node.textContent=message;node.className=`form-result ${ok?'success':'error'}`}
function initialPositionRows(){return [...document.querySelectorAll('[data-initial-position]')].map(row=>({symbol:row.dataset.initialPosition,enabled:row.querySelector('[data-position-enabled]').checked,shares:row.querySelector('[data-position-shares]').value,average_cost:row.querySelector('[data-position-cost]').value,planned_risk_per_share:row.querySelector('[data-position-risk]').value}))}
document.querySelector('#portfolio-initialize-form').addEventListener('submit',async event=>{event.preventDefault();const form=event.currentTarget,button=form.querySelector('[type="submit"]'),values=formPayload(form);let payload;try{const cash=finite(values.cash),risk=values.default_risk_per_trade===''?null:finite(values.default_risk_per_trade);if(cash==null||cash<0)throw new Error('初始现金无效');payload={name:values.name,cash,initial_positions:buildInitialPositions(initialPositionRows())};if(risk!=null)payload.default_risk_per_trade=risk}catch(error){showFormResult(form,error.message,false);return}button.disabled=true;try{await submitIntent(form.id,'/api/swing/portfolio/initialize',payload);showFormResult(form,'账户初始化记录已保存',true);await loadSnapshot(false)}catch(error){showFormResult(form,error.message,false)}finally{button.disabled=false}});
document.querySelector('#trade-form').addEventListener('submit',async event=>{event.preventDefault();const form=event.currentTarget,button=form.querySelector('[type="submit"]'),values=formPayload(form),local=new Date(values.executed_at);if(Number.isNaN(local.getTime())){showFormResult(form,'成交时间无效',false);return}const payload={symbol:values.symbol,side:values.side,shares:Number(values.shares),price:Number(values.price),fee:Number(values.fee),executed_at:local.toISOString()};if(values.planned_risk_per_share)payload.planned_risk_per_share=Number(values.planned_risk_per_share);if(values.exit_reason)payload.exit_reason=values.exit_reason;button.disabled=true;try{await submitIntent(form.id,'/api/swing/trades',payload);showFormResult(form,'成交记录已保存',true);await loadSnapshot(false)}catch(error){showFormResult(form,error.message,false)}finally{button.disabled=false}});
document.querySelector('#reverse-form').addEventListener('submit',async event=>{event.preventDefault();const form=event.currentTarget,button=form.querySelector('[type="submit"]'),id=String(new FormData(form).get('event_id')||'');if(!validEventId(id)){showFormResult(form,'成交事件 UUID 格式无效',false);return}button.disabled=true;try{await submitIntent(form.id,`/api/swing/trades/${encodeURIComponent(id)}/reverse`,{});showFormResult(form,'冲正记录已保存',true);await loadSnapshot(false)}catch(error){showFormResult(form,error.message,false)}finally{button.disabled=false}});
async function transitionAlert(button){const id=button.dataset.alertId,action=button.dataset.alertAction,node=button.closest('.alert-item')?.querySelector('.form-result');if(!canActOnAlert(state,button.dataset.alertScope,button.dataset.alertState)){if(node){node.textContent='安全暂停期间临时提醒不可操作';node.className='form-result error'}return}if(!validAlertId(id)||!['acknowledge','ignore'].includes(action)){if(node)node.textContent='提醒标识无效';return}button.disabled=true;try{await submitIntent(`${action}:${id}`,`/api/swing/alerts/${encodeURIComponent(id)}/${encodeURIComponent(action)}`,{});if(node){node.textContent=action==='acknowledge'?'已标记为已读':'已忽略本次';node.className='form-result success'}await loadSnapshot(false)}catch(error){if(node){node.textContent=error.message;node.className='form-result error'}}finally{button.disabled=false}}
function openTradeForAlert(symbol,scope,alertState){if(!canActOnAlert(state,scope,alertState)||!validSymbol(symbol))return;selectSymbol(symbol);const tab=document.querySelector('[data-panel="ledger-panel"]');if(tab)tab.click();document.querySelector('#trade-form [name="symbol"]').value=symbol;document.querySelector('#trade-form [name="shares"]').focus()}
permissionButton.addEventListener('click',async()=>{notificationOptIn=true;if(!('Notification'in window)){permissionButton.textContent='此浏览器不支持通知';renderHealth();return}try{const permission=await Notification.requestPermission();permissionButton.textContent=permission==='granted'?'通知已授权':permission==='denied'?'通知已拒绝':'通知未授权';renderHealth()}catch(error){setError(`通知授权失败：${error.message}`)}});
const tabs=[...document.querySelectorAll('[role="tab"]')];function activateTab(tab,{focus=false}={}){for(const current of tabs){const active=current===tab;current.classList.toggle('active',active);current.setAttribute('aria-selected',String(active));current.tabIndex=active?0:-1;document.querySelector('#'+current.dataset.panel).hidden=!active}if(tab.id==='backtest-tab')loadBacktest().catch(()=>{});if(focus)tab.focus()}for(const tab of tabs){tab.addEventListener('click',()=>activateTab(tab));tab.addEventListener('keydown',event=>{const index=tabs.indexOf(tab);let next=null;if(event.key==='ArrowRight')next=(index+1)%tabs.length;else if(event.key==='ArrowLeft')next=(index-1+tabs.length)%tabs.length;else if(event.key==='Home')next=0;else if(event.key==='End')next=tabs.length-1;if(next!=null){event.preventDefault();activateTab(tabs[next],{focus:true})}})}
backtestScopeNode.addEventListener('change',()=>{backtestLoadedKey=null;loadBacktest().catch(()=>{})});backtestRunNode.addEventListener('click',()=>loadBacktest(true).catch(()=>{}));
const tradeSymbol=document.querySelector('#trade-form [name="symbol"]'),initialRows=document.querySelector('#initial-position-rows');for(const [symbol,name] of VERIFIED_SYMBOLS){const option=document.createElement('option');option.value=symbol;option.textContent=`${name} ${symbol}`;tradeSymbol.append(option);const row=document.createElement('div');row.dataset.initialPosition=symbol;row.innerHTML=`<label class="toggle-label"><input data-position-enabled type="checkbox">${escapeHtml(name)} ${symbol}</label><label class="field"><span>份额</span><input data-position-shares type="number" min="100" step="100" disabled></label><label class="field"><span>平均成本</span><input data-position-cost type="number" min="0.001" step="0.001" disabled></label><label class="field"><span>每份计划风险</span><input data-position-risk type="number" min="0" step="0.001" value="0" disabled></label>`;const enabled=row.querySelector('[data-position-enabled]'),inputs=[...row.querySelectorAll('input:not([data-position-enabled])')];enabled.addEventListener('change',()=>{for(const input of inputs)input.disabled=!enabled.checked});initialRows.append(row)}
loadSnapshot(true).finally(connectEvents);
</script>
</body>
</html>
"""


__all__ = ["SWING_PAGE"]
